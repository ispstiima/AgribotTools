{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"AgribotTools","text":"<p>Utilities to convert datasets between Label Studio, YOLO and Ultralytics formats.</p> <p>AgribotTools is a Python toolkit developed by CNR STIIMA for converting computer-vision annotation datasets across popular formats used in object detection and segmentation tasks.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Format conversion \u2014 Convert seamlessly between Binary Mask, YOLO, Label Studio, and Ultralytics formats.</li> <li>:Validation \u2014 Automatically validate dataset structure before conversion.</li> </ul> <ul> <li>Gradio GUI \u2014 A web-based graphical interface for point-and-click conversions.</li> <li>CLI scripts \u2014 Ready-to-use command-line scripts for each conversion.</li> <li>Extensible architecture \u2014 Add new formats and conversions via simple decorators.</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Clone and install\ngit clone https://github.com/ispstiima/AgribotTools.git\ncd AgribotTools\nuv sync\n\n# Convert YOLO segmentation to Label Studio\npython scripts/yolo_to_ls.py seg /path/to/yolo_dataset --ls_base_name my_ls_output\n</code></pre> <p>For detailed setup instructions, see Getting Started.</p>"},{"location":"#supported-formats","title":"Supported Formats","text":"Format Description Binary Mask Binary segmentation masks YOLO Standard YOLO annotation format (segmentation &amp; detection) Label Studio JSON + XML format used by Label Studio Ultralytics Train/val/test split format used by Ultralytics <p>See the full Formats Overview and Conversion Matrix.</p>"},{"location":"#project-links","title":"Project Links","text":"<ul> <li>:fontawesome-brands-github: GitHub Repository</li> <li>:material-license: License (MIT)</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Thank you for your interest in contributing to AgribotTools! This guide explains how to set up a development environment and add new formats or conversions.</p>"},{"location":"contributing/#development-setup","title":"Development Setup","text":"<ol> <li> <p>Clone the repository and install in editable mode:</p> <pre><code>git clone https://github.com/LambdaLekter/AgribotTools.git\ncd AgribotTools\npip install -e .\n</code></pre> </li> <li> <p>Install documentation dependencies (optional, for building docs locally):</p> <pre><code>pip install mkdocs-material mkdocstrings[python]\n</code></pre> </li> <li> <p>Run the docs locally:</p> <pre><code>mkdocs serve\n</code></pre> </li> </ol>"},{"location":"contributing/#project-structure","title":"Project Structure","text":"<pre><code>AgribotTools/\n\u251c\u2500\u2500 src/\n\u2502   \u251c\u2500\u2500 cvtoolkit/\n\u2502   \u2502   \u251c\u2500\u2500 formats/        # Format definitions and validators\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 format.py   # Base classes: Format, FormatType, FormatRegistry\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 binmask.py  # Binary Mask format\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 yolo.py     # YOLO format\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 ls.py       # Label Studio format\n\u2502   \u2502   \u2502   \u2514\u2500\u2500 ul.py       # Ultralytics format\n\u2502   \u2502   \u251c\u2500\u2500 conversions/    # Conversion implementations\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 conversion.py  # Base: Conversion, ReversibleConversion\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 yolo_to_ls.py\n\u2502   \u2502   \u2502   \u251c\u2500\u2500 yolo_to_ul.py\n\u2502   \u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2502   \u251c\u2500\u2500 rle.py          # Run-length encoding utilities\n\u2502   \u2502   \u251c\u2500\u2500 mask.py         # Mask/contour utilities\n\u2502   \u2502   \u2514\u2500\u2500 colors.py       # Color palette for visualizations\n\u2502   \u251c\u2500\u2500 ui/                 # Gradio GUI helpers\n\u2502   \u2514\u2500\u2500 file_utils.py       # File copy utilities\n\u251c\u2500\u2500 scripts/                # CLI entry points\n\u251c\u2500\u2500 docs/                   # MkDocs documentation (this site)\n\u251c\u2500\u2500 gui.py                  # Gradio GUI entry point\n\u251c\u2500\u2500 mkdocs.yml              # MkDocs configuration\n\u2514\u2500\u2500 setup.cfg               # Package configuration\n</code></pre>"},{"location":"contributing/#adding-a-new-format","title":"Adding a New Format","text":"<p>Formats are registered using the <code>@register_format</code> decorator.</p>"},{"location":"contributing/#step-1-create-a-format-module","title":"Step 1: Create a format module","text":"<p>Create a new file in <code>src/cvtoolkit/formats/</code>, e.g., <code>coco.py</code>:</p> <pre><code>from pathlib import Path\nfrom typing import Tuple\nfrom cvtoolkit.formats.format import Format, FormatType, register_format\n\n\n@register_format(FormatType.COCO)  # Add COCO to FormatType enum first\nclass Coco(Format):\n    \"\"\"Validator for the COCO annotation format.\"\"\"\n\n    display_name = \"COCO\"\n\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"Check that the directory has the expected COCO structure.\"\"\"\n        annotations = self.path / \"annotations\"\n        images = self.path / \"images\"\n\n        if not annotations.exists():\n            return False, \"Missing 'annotations/' directory.\"\n        if not images.exists():\n            return False, \"Missing 'images/' directory.\"\n\n        return True, \"Valid COCO dataset.\"\n</code></pre>"},{"location":"contributing/#step-2-add-the-formattype-enum-value","title":"Step 2: Add the FormatType enum value","text":"<p>In <code>src/cvtoolkit/formats/format.py</code>, add the new value to <code>FormatType</code>:</p> <pre><code>class FormatType(Enum):\n    BINMASK = auto()\n    YOLO = auto()\n    LABEL_STUDIO = auto()\n    ULTRALYTICS = auto()\n    COCO = auto()  # New\n</code></pre>"},{"location":"contributing/#step-3-register-on-import","title":"Step 3: Register on import","text":"<p>In <code>src/cvtoolkit/__init__.py</code>, add the import to <code>initialize_registry()</code>:</p> <pre><code>def initialize_registry():\n    from cvtoolkit.formats import binmask, ls, ul, yolo, coco  # add coco\n    ...\n</code></pre>"},{"location":"contributing/#adding-a-new-conversion","title":"Adding a New Conversion","text":"<p>Conversions are registered using the <code>@register_conversion</code> decorator.</p>"},{"location":"contributing/#step-1-create-a-conversion-module","title":"Step 1: Create a conversion module","text":"<p>Create a new file in <code>src/cvtoolkit/conversions/</code>, e.g., <code>yolo_to_coco.py</code>:</p> <pre><code>from pathlib import Path\nfrom cvtoolkit.conversions.conversion import (\n    Conversion, \n    ReversibleConversion, \n    register_conversion\n)\nfrom cvtoolkit.formats.format import FormatType\n\n\n@register_conversion(FormatType.YOLO, FormatType.COCO)\nclass YoloToCoco(Conversion):\n    \"\"\"Convert YOLO format to COCO format.\"\"\"\n\n    def convert(self, **kwargs) -&gt; Path:\n        \"\"\"Perform the YOLO \u2192 COCO conversion.\"\"\"\n        # Create output directory\n        output_dir = self.target_path\n        output_dir.mkdir(parents=True, exist_ok=True)\n        self._track_path(output_dir)\n\n        # ... conversion logic ...\n\n        self._report_progress(1.0, \"Conversion complete.\")\n        return output_dir\n</code></pre>"},{"location":"contributing/#step-2-register-on-import","title":"Step 2: Register on import","text":"<p>In <code>src/cvtoolkit/__init__.py</code>, add the import:</p> <pre><code>def initialize_registry():\n    ...\n    from cvtoolkit.conversions import yolo_to_coco  # add new conversion\n</code></pre>"},{"location":"contributing/#step-3-create-a-cli-script-optional","title":"Step 3: Create a CLI script (optional)","text":"<p>Create <code>scripts/yolo_to_coco.py</code> following the pattern of existing scripts.</p>"},{"location":"contributing/#coding-conventions","title":"Coding Conventions","text":"<ul> <li>Docstrings: Use Google-style docstrings. These are automatically picked up by <code>mkdocstrings</code>.</li> <li>Type hints: All public functions should have type annotations.</li> <li>Logging: Use the module-level logger (<code>log = logging.getLogger(\"...\")</code>) instead of <code>print()</code>.</li> <li>Progress tracking: Call <code>self._report_progress(fraction, message)</code> during long operations.</li> <li>Rollback support: Call <code>self._track_path(path)</code> for any files/directories your conversion creates.</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python &gt;= 3.8</li> <li>uv (Python package manager)</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>We recommend using a virtual environment managed by <code>uv</code>.</p> <pre><code># Clone and sync\ngit clone https://github.com/LambdaLekter/AgribotTools.git\ncd AgribotTools\nuv sync\n</code></pre> <p>This will automatically create a virtual environment and install all dependencies from <code>pyproject.toml</code> and <code>uv.lock</code>.</p>"},{"location":"getting-started/#label-studio-setup","title":"Label Studio Setup","text":"<p>If Label Studio is not installed, follow the official installation guide.</p>"},{"location":"getting-started/#first-conversion","title":"First Conversion","text":"<p>Once installed, try a conversion using one of the CLI scripts:</p> <pre><code># Convert from YOLO segmentation to Label Studio format\npython scripts/yolo_to_ls.py seg /path/to/yolo_dataset --ls_base_name my_ls_output\n</code></pre> <p>To see all available options for any script:</p> <pre><code>python scripts/&lt;script_name&gt;.py -h\n</code></pre> <p>For more details, see the Conversions Usage Guide.</p>"},{"location":"api/conversions/","title":"API Reference \u2014 Conversions","text":"<p>This page documents the conversion base classes, error types, and decorators.</p>"},{"location":"api/conversions/#conversion-base-class","title":"Conversion (Base Class)","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion","title":"<code>cvtoolkit.conversions.conversion.Conversion</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for all format conversions.</p> <p>Provides: - Source/target path management - Validation before conversion - Rollback support on failure - Progress tracking hooks</p> <p>Attributes:</p> Name Type Description <code>source_path</code> <p>Path to the source dataset</p> <code>target_path</code> <p>Path where converted dataset will be saved</p> <code>source_type</code> <code>FormatType</code> <p>FormatType of the source (set by decorator)</p> <code>target_type</code> <code>FormatType</code> <p>FormatType of the target (set by decorator)</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>class Conversion(abc.ABC):\n    \"\"\"\n    Abstract base class for all format conversions.\n\n    Provides:\n    - Source/target path management\n    - Validation before conversion\n    - Rollback support on failure\n    - Progress tracking hooks\n\n    Attributes:\n        source_path: Path to the source dataset\n        target_path: Path where converted dataset will be saved\n        source_type: FormatType of the source (set by decorator)\n        target_type: FormatType of the target (set by decorator)\n    \"\"\"\n\n    source_type: FormatType = None  # Set by decorator\n    target_type: FormatType = None  # Set by decorator\n\n    def __init__(self, source_path: str | Path, target_path: str | Path, task_type: TaskType=TaskType.GENERIC):\n        \"\"\"\n        Initialize the conversion.\n\n        Args:\n            source_path: Path to the source dataset directory\n            target_path: Path where the converted dataset will be created\n            task_type: Type of the Object Detection task described by the target dataset\n        \"\"\"\n        self.source_path = Path(source_path)\n        self.target_path = Path(target_path)\n        self.task_type = task_type\n        self._rollback_paths: list[Path] = []\n        self._progress_callback: Optional[Callable[[float, str], None]] = None\n\n    def set_progress_callback(self, callback: Callable[[float, str], None]) -&gt; None:\n        \"\"\"\n        Set a callback for progress updates.\n\n        Args:\n            callback: Function that receives (progress: float 0-1, message: str)\n        \"\"\"\n        self._progress_callback = callback\n\n    def _report_progress(self, progress: float, message: str) -&gt; None:\n        \"\"\"Report progress to the callback if set.\"\"\"\n        if self._progress_callback:\n            self._progress_callback(progress, message)\n\n    def _sub_progress_callback(self, start: float, end: float):\n        \"\"\"Create a callback that maps 0.0-1.0 to a sub-range of the parent progress.\"\"\"\n        def callback(progress, message):\n            mapped = start + (end - start) * progress\n            self._report_progress(mapped, message)\n        return callback\n\n    def validate_source(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate the source dataset.\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        format_class = FormatRegistry.get_format_class(self.source_type)\n        if format_class is None:\n            return False, f\"No format validator registered for {self.source_type}\"\n\n        validator = format_class(self.source_path)\n        return validator.validate()\n\n    def validate_target_path(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate that the target path is suitable for output.\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        if self.target_path.exists():\n            if not self.target_path.is_dir():\n                return False, f\"Target path '{self.target_path}' exists but is not a directory.\"\n            if any(self.target_path.iterdir()):\n                return False, f\"Target path '{self.target_path}' already exists and is not empty.\"\n        return True, \"\"\n\n    @contextmanager\n    def _rollback_context(self):\n        \"\"\"\n        Context manager that tracks created paths and rolls back on exception.\n\n        Usage:\n            with self._rollback_context():\n                self._track_path(new_dir)\n                # ... do conversion work ...\n        \"\"\"\n        self._rollback_paths = []\n        try:\n            yield\n        except Exception as e:\n            log.error(f\"Conversion failed: {e}. Rolling back...\")\n            self._rollback()\n            raise ConversionError(f\"Conversion failed: {e}\") from e\n\n    def _track_path(self, path: Path) -&gt; None:\n        \"\"\"Track a path for potential rollback.\"\"\"\n        self._rollback_paths.append(path)\n\n    def _rollback(self) -&gt; None:\n        \"\"\"Remove all tracked paths in reverse order.\"\"\"\n        for path in reversed(self._rollback_paths):\n            try:\n                if path.exists():\n                    if path.is_dir():\n                        shutil.rmtree(path)\n                        log.info(f\"Rolled back directory: {path}\")\n                    else:\n                        path.unlink()\n                        log.info(f\"Rolled back file: {path}\")\n            except Exception as e:\n                log.warning(f\"Failed to rollback {path}: {e}\")\n        self._rollback_paths = []\n\n    def run(self, **kwargs) -&gt; Path:\n        \"\"\"\n        Execute the conversion with validation and rollback support.\n\n        This is the main entry point that:\n        1. Validates the source dataset\n        2. Validates the target path\n        3. Runs the conversion\n        4. Rolls back on failure\n\n        Args:\n            **kwargs: Additional arguments passed to convert()\n\n        Returns:\n            Path to the converted dataset\n\n        Raises:\n            ConversionError: If validation or conversion fails\n        \"\"\"\n        # Validate source\n        self._report_progress(0.0, \"Validating source dataset...\")\n        valid, message = self.validate_source()\n        if not valid:\n            raise ConversionError(f\"Source validation failed: {message}\")\n\n        # Validate target\n        self._report_progress(0.1, \"Validating target path...\")\n        valid, message = self.validate_target_path()\n        if not valid:\n            raise ConversionError(f\"Target validation failed: {message}\")\n\n        # Run conversion with rollback support\n        self._report_progress(0.2, \"Starting conversion...\")\n        with self._rollback_context():\n            result = self.convert(**kwargs)\n\n        self._report_progress(1.0, \"Conversion complete!\")\n        return result\n\n    @abc.abstractmethod\n    def convert(self, **kwargs) -&gt; Path:\n        \"\"\"\n        Perform the actual conversion.\n\n        Must be implemented by subclasses. Should use self._track_path()\n        to register created directories/files for rollback.\n\n        Args:\n            **kwargs: Conversion-specific arguments\n\n        Returns:\n            Path to the converted dataset\n        \"\"\"\n        pass\n\n    @classmethod\n    def get_display_name(cls) -&gt; str:\n        \"\"\"Get a human-readable name for this conversion.\"\"\"\n        if cls.source_type and cls.target_type:\n            src = FormatRegistry.get_display_name(cls.source_type)\n            tgt = FormatRegistry.get_display_name(cls.target_type)\n            return f\"{src} \u2192 {tgt}\"\n        return cls.__name__\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.source_type","title":"<code>source_type = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.target_type","title":"<code>target_type = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.source_path","title":"<code>source_path = Path(source_path)</code>  <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.target_path","title":"<code>target_path = Path(target_path)</code>  <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.task_type","title":"<code>task_type = task_type</code>  <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._rollback_paths","title":"<code>_rollback_paths = []</code>  <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._progress_callback","title":"<code>_progress_callback = None</code>  <code>instance-attribute</code>","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.__init__","title":"<code>__init__(source_path, target_path, task_type=TaskType.GENERIC)</code>","text":"<p>Initialize the conversion.</p> <p>Parameters:</p> Name Type Description Default <code>source_path</code> <code>str | Path</code> <p>Path to the source dataset directory</p> required <code>target_path</code> <code>str | Path</code> <p>Path where the converted dataset will be created</p> required <code>task_type</code> <code>TaskType</code> <p>Type of the Object Detection task described by the target dataset</p> <code>GENERIC</code> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def __init__(self, source_path: str | Path, target_path: str | Path, task_type: TaskType=TaskType.GENERIC):\n    \"\"\"\n    Initialize the conversion.\n\n    Args:\n        source_path: Path to the source dataset directory\n        target_path: Path where the converted dataset will be created\n        task_type: Type of the Object Detection task described by the target dataset\n    \"\"\"\n    self.source_path = Path(source_path)\n    self.target_path = Path(target_path)\n    self.task_type = task_type\n    self._rollback_paths: list[Path] = []\n    self._progress_callback: Optional[Callable[[float, str], None]] = None\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.set_progress_callback","title":"<code>set_progress_callback(callback)</code>","text":"<p>Set a callback for progress updates.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable[[float, str], None]</code> <p>Function that receives (progress: float 0-1, message: str)</p> required Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def set_progress_callback(self, callback: Callable[[float, str], None]) -&gt; None:\n    \"\"\"\n    Set a callback for progress updates.\n\n    Args:\n        callback: Function that receives (progress: float 0-1, message: str)\n    \"\"\"\n    self._progress_callback = callback\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._report_progress","title":"<code>_report_progress(progress, message)</code>","text":"<p>Report progress to the callback if set.</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def _report_progress(self, progress: float, message: str) -&gt; None:\n    \"\"\"Report progress to the callback if set.\"\"\"\n    if self._progress_callback:\n        self._progress_callback(progress, message)\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._sub_progress_callback","title":"<code>_sub_progress_callback(start, end)</code>","text":"<p>Create a callback that maps 0.0-1.0 to a sub-range of the parent progress.</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def _sub_progress_callback(self, start: float, end: float):\n    \"\"\"Create a callback that maps 0.0-1.0 to a sub-range of the parent progress.\"\"\"\n    def callback(progress, message):\n        mapped = start + (end - start) * progress\n        self._report_progress(mapped, message)\n    return callback\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.validate_source","title":"<code>validate_source()</code>","text":"<p>Validate the source dataset.</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def validate_source(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate the source dataset.\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    format_class = FormatRegistry.get_format_class(self.source_type)\n    if format_class is None:\n        return False, f\"No format validator registered for {self.source_type}\"\n\n    validator = format_class(self.source_path)\n    return validator.validate()\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.validate_target_path","title":"<code>validate_target_path()</code>","text":"<p>Validate that the target path is suitable for output.</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def validate_target_path(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate that the target path is suitable for output.\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    if self.target_path.exists():\n        if not self.target_path.is_dir():\n            return False, f\"Target path '{self.target_path}' exists but is not a directory.\"\n        if any(self.target_path.iterdir()):\n            return False, f\"Target path '{self.target_path}' already exists and is not empty.\"\n    return True, \"\"\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._rollback_context","title":"<code>_rollback_context()</code>","text":"<p>Context manager that tracks created paths and rolls back on exception.</p> Usage <p>with self.rollbackcontext():     self.trackpath(new_dir)     # ... do conversion work ...</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>@contextmanager\ndef _rollback_context(self):\n    \"\"\"\n    Context manager that tracks created paths and rolls back on exception.\n\n    Usage:\n        with self._rollback_context():\n            self._track_path(new_dir)\n            # ... do conversion work ...\n    \"\"\"\n    self._rollback_paths = []\n    try:\n        yield\n    except Exception as e:\n        log.error(f\"Conversion failed: {e}. Rolling back...\")\n        self._rollback()\n        raise ConversionError(f\"Conversion failed: {e}\") from e\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._track_path","title":"<code>_track_path(path)</code>","text":"<p>Track a path for potential rollback.</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def _track_path(self, path: Path) -&gt; None:\n    \"\"\"Track a path for potential rollback.\"\"\"\n    self._rollback_paths.append(path)\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion._rollback","title":"<code>_rollback()</code>","text":"<p>Remove all tracked paths in reverse order.</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def _rollback(self) -&gt; None:\n    \"\"\"Remove all tracked paths in reverse order.\"\"\"\n    for path in reversed(self._rollback_paths):\n        try:\n            if path.exists():\n                if path.is_dir():\n                    shutil.rmtree(path)\n                    log.info(f\"Rolled back directory: {path}\")\n                else:\n                    path.unlink()\n                    log.info(f\"Rolled back file: {path}\")\n        except Exception as e:\n            log.warning(f\"Failed to rollback {path}: {e}\")\n    self._rollback_paths = []\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.run","title":"<code>run(**kwargs)</code>","text":"<p>Execute the conversion with validation and rollback support.</p> <p>This is the main entry point that: 1. Validates the source dataset 2. Validates the target path 3. Runs the conversion 4. Rolls back on failure</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <p>Additional arguments passed to convert()</p> <code>{}</code> <p>Returns:</p> Type Description <code>Path</code> <p>Path to the converted dataset</p> <p>Raises:</p> Type Description <code>ConversionError</code> <p>If validation or conversion fails</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def run(self, **kwargs) -&gt; Path:\n    \"\"\"\n    Execute the conversion with validation and rollback support.\n\n    This is the main entry point that:\n    1. Validates the source dataset\n    2. Validates the target path\n    3. Runs the conversion\n    4. Rolls back on failure\n\n    Args:\n        **kwargs: Additional arguments passed to convert()\n\n    Returns:\n        Path to the converted dataset\n\n    Raises:\n        ConversionError: If validation or conversion fails\n    \"\"\"\n    # Validate source\n    self._report_progress(0.0, \"Validating source dataset...\")\n    valid, message = self.validate_source()\n    if not valid:\n        raise ConversionError(f\"Source validation failed: {message}\")\n\n    # Validate target\n    self._report_progress(0.1, \"Validating target path...\")\n    valid, message = self.validate_target_path()\n    if not valid:\n        raise ConversionError(f\"Target validation failed: {message}\")\n\n    # Run conversion with rollback support\n    self._report_progress(0.2, \"Starting conversion...\")\n    with self._rollback_context():\n        result = self.convert(**kwargs)\n\n    self._report_progress(1.0, \"Conversion complete!\")\n    return result\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.convert","title":"<code>convert(**kwargs)</code>  <code>abstractmethod</code>","text":"<p>Perform the actual conversion.</p> <p>Must be implemented by subclasses. Should use self.trackpath() to register created directories/files for rollback.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <p>Conversion-specific arguments</p> <code>{}</code> <p>Returns:</p> Type Description <code>Path</code> <p>Path to the converted dataset</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>@abc.abstractmethod\ndef convert(self, **kwargs) -&gt; Path:\n    \"\"\"\n    Perform the actual conversion.\n\n    Must be implemented by subclasses. Should use self._track_path()\n    to register created directories/files for rollback.\n\n    Args:\n        **kwargs: Conversion-specific arguments\n\n    Returns:\n        Path to the converted dataset\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/conversions/#cvtoolkit.conversions.conversion.Conversion.get_display_name","title":"<code>get_display_name()</code>  <code>classmethod</code>","text":"<p>Get a human-readable name for this conversion.</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>@classmethod\ndef get_display_name(cls) -&gt; str:\n    \"\"\"Get a human-readable name for this conversion.\"\"\"\n    if cls.source_type and cls.target_type:\n        src = FormatRegistry.get_display_name(cls.source_type)\n        tgt = FormatRegistry.get_display_name(cls.target_type)\n        return f\"{src} \u2192 {tgt}\"\n    return cls.__name__\n</code></pre>"},{"location":"api/conversions/#conversionerror","title":"ConversionError","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.ConversionError","title":"<code>cvtoolkit.conversions.conversion.ConversionError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when a conversion fails.</p>"},{"location":"api/conversions/#decorators","title":"Decorators","text":""},{"location":"api/conversions/#register_conversion","title":"register_conversion","text":""},{"location":"api/conversions/#cvtoolkit.conversions.conversion.register_conversion","title":"<code>cvtoolkit.conversions.conversion.register_conversion(source, target)</code>","text":"<p>Decorator to register a conversion class with the format registry.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>FormatType</code> <p>The source format type</p> required <code>target</code> <code>FormatType</code> <p>The target format type</p> required Usage <p>@registerconversion(FormatType.YOLOSEG, FormatType.LABELSTUDIOSEG) class YoloSegToLabelStudioSeg(Conversion):     ...</p> Source code in <code>src/cvtoolkit/conversions/conversion.py</code> <pre><code>def register_conversion(source: FormatType, target: FormatType):\n    \"\"\"\n    Decorator to register a conversion class with the format registry.\n\n    Args:\n        source: The source format type\n        target: The target format type\n\n    Usage:\n        @register_conversion(FormatType.YOLO_SEG, FormatType.LABEL_STUDIO_SEG)\n        class YoloSegToLabelStudioSeg(Conversion):\n            ...\n    \"\"\"\n    def decorator(cls: Type[\"Conversion\"]) -&gt; Type[\"Conversion\"]:\n        FormatRegistry.register_conversion(source, target, cls)\n        cls.source_type = source\n        cls.target_type = target\n        return cls\n    return decorator\n</code></pre>"},{"location":"api/conversions/#register_format","title":"register_format","text":""},{"location":"api/conversions/#cvtoolkit.formats.format.register_format","title":"<code>cvtoolkit.formats.format.register_format(format_type)</code>","text":"<p>Decorator to register a format class with the registry.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>def register_format(format_type: FormatType):\n    \"\"\"Decorator to register a format class with the registry.\"\"\"\n    def decorator(cls: Type[\"Format\"]) -&gt; Type[\"Format\"]:\n        FormatRegistry.register_format(format_type, cls)\n        cls.format_type = format_type\n        return cls\n    return decorator\n</code></pre>"},{"location":"api/formats/","title":"API Reference \u2014 Formats","text":"<p>This page documents the core format classes and the format registry.</p>"},{"location":"api/formats/#formattype","title":"FormatType","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatType","title":"<code>cvtoolkit.formats.format.FormatType</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Enumeration of supported annotation format types.</p>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatType.BINMASK","title":"<code>BINMASK = auto()</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatType.YOLO","title":"<code>YOLO = auto()</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatType.LABEL_STUDIO","title":"<code>LABEL_STUDIO = auto()</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatType.ULTRALYTICS","title":"<code>ULTRALYTICS = auto()</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#tasktype","title":"TaskType","text":""},{"location":"api/formats/#cvtoolkit.formats.TaskType","title":"<code>cvtoolkit.formats.TaskType</code>","text":"<p>               Bases: <code>Enum</code></p>"},{"location":"api/formats/#cvtoolkit.formats.TaskType.GENERIC","title":"<code>GENERIC = 'Generic'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.TaskType.DETECTION","title":"<code>DETECTION = 'Detection'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.TaskType.SEGMENTATION","title":"<code>SEGMENTATION = 'Segmentation'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#formatregistry","title":"FormatRegistry","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry","title":"<code>cvtoolkit.formats.format.FormatRegistry</code>","text":"<p>Central registry for format classes and their supported conversions.</p> <p>This registry enables: - Dynamic discovery of available formats - Lookup of supported target formats for any source format - GUI dropdown population based on available conversions</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>class FormatRegistry:\n    \"\"\"\n    Central registry for format classes and their supported conversions.\n\n    This registry enables:\n    - Dynamic discovery of available formats\n    - Lookup of supported target formats for any source format\n    - GUI dropdown population based on available conversions\n    \"\"\"\n\n    _formats: Dict[FormatType, Type[\"Format\"]] = {}\n    _conversions: Dict[Tuple[FormatType, FormatType], Type[\"Conversion\"]] = {}\n    _display_names: Dict[FormatType, str] = {\n        FormatType.BINMASK: \"Binary Mask\",\n        FormatType.YOLO: \"YOLO\",\n        FormatType.LABEL_STUDIO: \"Label Studio\",\n        FormatType.ULTRALYTICS: \"Ultralytics\",\n    }\n\n    @classmethod\n    def register_format(cls, format_type: FormatType, format_class: Type[\"Format\"]) -&gt; None:\n        \"\"\"Register a format class for a given format type.\"\"\"\n        cls._formats[format_type] = format_class\n\n    @classmethod\n    def register_conversion(\n        cls, \n        source: FormatType, \n        target: FormatType, \n        conversion_class: Type[\"Conversion\"]\n    ) -&gt; None:\n        \"\"\"Register a conversion between two format types.\"\"\"\n        cls._conversions[(source, target)] = conversion_class\n\n    @classmethod\n    def get_format_class(cls, format_type: FormatType) -&gt; Optional[Type[\"Format\"]]:\n        \"\"\"Get the format class for a given format type.\"\"\"\n        return cls._formats.get(format_type)\n\n    @classmethod\n    def get_conversion_class(\n        cls, \n        source: FormatType, \n        target: FormatType\n    ) -&gt; Optional[Type[\"Conversion\"]]:\n        \"\"\"Get the conversion class for a source-target pair.\"\"\"\n        return cls._conversions.get((source, target))\n\n    @classmethod\n    def get_supported_targets(cls, source: FormatType) -&gt; List[FormatType]:\n        \"\"\"Get all formats that the source format can be converted to.\"\"\"\n        return [\n            target for (src, target) in cls._conversions.keys() \n            if src == source\n        ]\n\n    @classmethod\n    def get_all_source_formats(cls) -&gt; List[FormatType]:\n        \"\"\"Get all formats that can be used as conversion sources.\"\"\"\n        return list(set(src for src, _ in cls._conversions.keys()))\n\n    @classmethod\n    def get_display_name(cls, format_type: FormatType) -&gt; str:\n        \"\"\"Get human-readable display name for a format type.\"\"\"\n        return cls._display_names.get(format_type, format_type.name)\n\n    @classmethod\n    def get_format_choices(cls) -&gt; List[Tuple[str, FormatType]]:\n        \"\"\"Get list of (display_name, format_type) tuples for GUI dropdowns.\"\"\"\n        sources = cls.get_all_source_formats()\n        return [(cls.get_display_name(fmt), fmt) for fmt in sources]\n\n    @classmethod\n    def get_target_choices(cls, source: FormatType) -&gt; List[Tuple[str, FormatType]]:\n        \"\"\"Get list of valid target formats for a given source format.\"\"\"\n        targets = cls.get_supported_targets(source)\n        return [(cls.get_display_name(fmt), fmt) for fmt in targets]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry._formats","title":"<code>_formats = {}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry._conversions","title":"<code>_conversions = {}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry._display_names","title":"<code>_display_names = {FormatType.BINMASK: 'Binary Mask', FormatType.YOLO: 'YOLO', FormatType.LABEL_STUDIO: 'Label Studio', FormatType.ULTRALYTICS: 'Ultralytics'}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.register_format","title":"<code>register_format(format_type, format_class)</code>  <code>classmethod</code>","text":"<p>Register a format class for a given format type.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef register_format(cls, format_type: FormatType, format_class: Type[\"Format\"]) -&gt; None:\n    \"\"\"Register a format class for a given format type.\"\"\"\n    cls._formats[format_type] = format_class\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.register_conversion","title":"<code>register_conversion(source, target, conversion_class)</code>  <code>classmethod</code>","text":"<p>Register a conversion between two format types.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef register_conversion(\n    cls, \n    source: FormatType, \n    target: FormatType, \n    conversion_class: Type[\"Conversion\"]\n) -&gt; None:\n    \"\"\"Register a conversion between two format types.\"\"\"\n    cls._conversions[(source, target)] = conversion_class\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_format_class","title":"<code>get_format_class(format_type)</code>  <code>classmethod</code>","text":"<p>Get the format class for a given format type.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_format_class(cls, format_type: FormatType) -&gt; Optional[Type[\"Format\"]]:\n    \"\"\"Get the format class for a given format type.\"\"\"\n    return cls._formats.get(format_type)\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_conversion_class","title":"<code>get_conversion_class(source, target)</code>  <code>classmethod</code>","text":"<p>Get the conversion class for a source-target pair.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_conversion_class(\n    cls, \n    source: FormatType, \n    target: FormatType\n) -&gt; Optional[Type[\"Conversion\"]]:\n    \"\"\"Get the conversion class for a source-target pair.\"\"\"\n    return cls._conversions.get((source, target))\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_supported_targets","title":"<code>get_supported_targets(source)</code>  <code>classmethod</code>","text":"<p>Get all formats that the source format can be converted to.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_supported_targets(cls, source: FormatType) -&gt; List[FormatType]:\n    \"\"\"Get all formats that the source format can be converted to.\"\"\"\n    return [\n        target for (src, target) in cls._conversions.keys() \n        if src == source\n    ]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_all_source_formats","title":"<code>get_all_source_formats()</code>  <code>classmethod</code>","text":"<p>Get all formats that can be used as conversion sources.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_all_source_formats(cls) -&gt; List[FormatType]:\n    \"\"\"Get all formats that can be used as conversion sources.\"\"\"\n    return list(set(src for src, _ in cls._conversions.keys()))\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_display_name","title":"<code>get_display_name(format_type)</code>  <code>classmethod</code>","text":"<p>Get human-readable display name for a format type.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_display_name(cls, format_type: FormatType) -&gt; str:\n    \"\"\"Get human-readable display name for a format type.\"\"\"\n    return cls._display_names.get(format_type, format_type.name)\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_format_choices","title":"<code>get_format_choices()</code>  <code>classmethod</code>","text":"<p>Get list of (displayname, formattype) tuples for GUI dropdowns.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_format_choices(cls) -&gt; List[Tuple[str, FormatType]]:\n    \"\"\"Get list of (display_name, format_type) tuples for GUI dropdowns.\"\"\"\n    sources = cls.get_all_source_formats()\n    return [(cls.get_display_name(fmt), fmt) for fmt in sources]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.FormatRegistry.get_target_choices","title":"<code>get_target_choices(source)</code>  <code>classmethod</code>","text":"<p>Get list of valid target formats for a given source format.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_target_choices(cls, source: FormatType) -&gt; List[Tuple[str, FormatType]]:\n    \"\"\"Get list of valid target formats for a given source format.\"\"\"\n    targets = cls.get_supported_targets(source)\n    return [(cls.get_display_name(fmt), fmt) for fmt in targets]\n</code></pre>"},{"location":"api/formats/#format-base-class","title":"Format (Base Class)","text":""},{"location":"api/formats/#cvtoolkit.formats.format.Format","title":"<code>cvtoolkit.formats.format.Format</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for dataset format validation.</p> <p>Subclasses must implement validate_structure() to check if a directory conforms to the expected format structure.</p> <p>Attributes:</p> Name Type Description <code>path</code> <p>Path to the dataset directory</p> <code>format_type</code> <code>FormatType</code> <p>The FormatType enum value (set by @register_format decorator)</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>class Format(abc.ABC):\n    \"\"\"\n    Abstract base class for dataset format validation.\n\n    Subclasses must implement validate_structure() to check if a directory\n    conforms to the expected format structure.\n\n    Attributes:\n        path: Path to the dataset directory\n        format_type: The FormatType enum value (set by @register_format decorator)\n    \"\"\"\n\n    format_type: FormatType = None  # Set by decorator\n\n    def __init__(self, path: str | Path):\n        \"\"\"\n        Initialize the format validator.\n\n        Args:\n            path: Path to the dataset directory to validate\n        \"\"\"\n        self.path = Path(path)\n\n    def validate(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate the dataset directory.\n\n        Performs basic existence check, then delegates to format-specific validation.\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        if not self.path.exists():\n            return False, f\"The path '{self.path}' does not exist.\"\n        if not self.path.is_dir():\n            return False, f\"The path '{self.path}' is not a directory.\"\n        return self.validate_structure()\n\n    @abc.abstractmethod\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate the format-specific directory structure.\n\n        Must be implemented by subclasses to check for required files and folders.\n\n        Returns:\n            Tuple of (success: bool, message: str describing the issue if failed)\n        \"\"\"\n        pass\n\n    @classmethod\n    def get_display_name(cls) -&gt; str:\n        \"\"\"Get the human-readable name for this format.\"\"\"\n        if cls.format_type:\n            return FormatRegistry.get_display_name(cls.format_type)\n        return cls.__name__\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.Format.format_type","title":"<code>format_type = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.Format.path","title":"<code>path = Path(path)</code>  <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.format.Format.__init__","title":"<code>__init__(path)</code>","text":"<p>Initialize the format validator.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Path to the dataset directory to validate</p> required Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>def __init__(self, path: str | Path):\n    \"\"\"\n    Initialize the format validator.\n\n    Args:\n        path: Path to the dataset directory to validate\n    \"\"\"\n    self.path = Path(path)\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.Format.validate","title":"<code>validate()</code>","text":"<p>Validate the dataset directory.</p> <p>Performs basic existence check, then delegates to format-specific validation.</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>def validate(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate the dataset directory.\n\n    Performs basic existence check, then delegates to format-specific validation.\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    if not self.path.exists():\n        return False, f\"The path '{self.path}' does not exist.\"\n    if not self.path.is_dir():\n        return False, f\"The path '{self.path}' is not a directory.\"\n    return self.validate_structure()\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.Format.validate_structure","title":"<code>validate_structure()</code>  <code>abstractmethod</code>","text":"<p>Validate the format-specific directory structure.</p> <p>Must be implemented by subclasses to check for required files and folders.</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str describing the issue if failed)</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@abc.abstractmethod\ndef validate_structure(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate the format-specific directory structure.\n\n    Must be implemented by subclasses to check for required files and folders.\n\n    Returns:\n        Tuple of (success: bool, message: str describing the issue if failed)\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.format.Format.get_display_name","title":"<code>get_display_name()</code>  <code>classmethod</code>","text":"<p>Get the human-readable name for this format.</p> Source code in <code>src/cvtoolkit/formats/format.py</code> <pre><code>@classmethod\ndef get_display_name(cls) -&gt; str:\n    \"\"\"Get the human-readable name for this format.\"\"\"\n    if cls.format_type:\n        return FormatRegistry.get_display_name(cls.format_type)\n    return cls.__name__\n</code></pre>"},{"location":"api/formats/#format-implementations","title":"Format Implementations","text":""},{"location":"api/formats/#binmask","title":"Binmask","text":""},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask","title":"<code>cvtoolkit.formats.binmask.Binmask</code>","text":"<p>               Bases: <code>Format</code></p> <p>Binary mask format for segmentation datasets.</p> <p>Binary masks are binary images where each pixel indicates whether it belongs to the foreground (255) or background (0).</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>@register_format(FormatType.BINMASK)\nclass Binmask(Format):\n    \"\"\"\n    Binary mask format for segmentation datasets.\n\n    Binary masks are binary images where each pixel indicates whether\n    it belongs to the foreground (255) or background (0).\n    \"\"\"\n\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate that the directory contains required binary mask structure.\n\n        Checks for:\n        - images/ directory with image files\n        - labels/ directory with mask files\n        - classes.txt file (optional but recommended)\n        - Matching image/mask file pairs\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        images_dir = self.path / \"images\"\n        labels_dir = self.path / \"labels\"\n\n        # Check required directories\n        if not images_dir.exists():\n            return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n        if not labels_dir.exists():\n            return False, f\"Missing 'labels' directory in '{self.path}'.\"\n\n        # Get file stems\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        mask_extensions = {\".png\", \".jpg\", \".jpeg\", \".bmp\", \".tiff\", \".tif\"}\n\n        image_stems = {\n            f.stem for f in images_dir.iterdir()\n            if f.is_file() and f.suffix.lower() in image_extensions\n        }\n        mask_stems = {\n            f.stem for f in labels_dir.iterdir()\n            if f.is_file() and f.suffix.lower() in mask_extensions\n        }\n\n        if not image_stems:\n            return False, f\"No image files found in '{images_dir}'.\"\n\n        if not mask_stems:\n            return False, f\"No mask files found in '{labels_dir}'.\"\n\n        # Check for matching files\n        missing_masks = image_stems - mask_stems\n        missing_images = mask_stems - image_stems\n\n        if missing_masks:\n            return False, f\"Missing binary masks in '{labels_dir}' for {len(missing_masks)} images.\"\n\n        if missing_images:\n            return False, f\"Missing images in '{images_dir}' for {len(missing_images)} masks.\"\n\n        return True, \"\"\n\n    def get_classes(self) -&gt; Optional[dict]:\n        \"\"\"Get class names from classes.txt if it exists.\"\"\"\n        classes_file = self.path / \"classes.txt\"\n        if not classes_file.exists():\n            return None\n\n        try:\n            with open(classes_file, 'r') as f:\n                class_names = [line.strip() for line in f.readlines() if line.strip()]\n            return {i: name for i, name in enumerate(class_names)}\n        except Exception:\n            return None\n\n    def get_image_paths(self) -&gt; List[Path]:\n        \"\"\"Get list of original image file paths.\"\"\"\n        images_dir = self.path / \"images\"\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        return [\n            f for f in images_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in image_extensions\n        ]\n\n    def get_mask_path(self, image_path: Path) -&gt; Optional[Path]:\n        \"\"\"Get the mask file path for a given image.\"\"\"\n        labels_dir = self.path / \"labels\"\n        mask_extensions = [\".png\", \".jpg\", \".jpeg\", \".bmp\", \".tiff\", \".tif\"]\n\n        for ext in mask_extensions:\n            mask_path = labels_dir / f\"{image_path.stem}{ext}\"\n            if mask_path.exists():\n                return mask_path\n        return None\n\n    def get_mask_paths(self) -&gt; List[Path]:\n        \"\"\"Get list of mask file paths.\"\"\"\n        labels_dir = self.path / \"labels\"\n        mask_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        return [\n            f for f in labels_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in mask_extensions\n        ]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask.validate_structure","title":"<code>validate_structure()</code>","text":"<p>Validate that the directory contains required binary mask structure.</p> <p>Checks for: - images/ directory with image files - labels/ directory with mask files - classes.txt file (optional but recommended) - Matching image/mask file pairs</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>def validate_structure(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate that the directory contains required binary mask structure.\n\n    Checks for:\n    - images/ directory with image files\n    - labels/ directory with mask files\n    - classes.txt file (optional but recommended)\n    - Matching image/mask file pairs\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    images_dir = self.path / \"images\"\n    labels_dir = self.path / \"labels\"\n\n    # Check required directories\n    if not images_dir.exists():\n        return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n    if not labels_dir.exists():\n        return False, f\"Missing 'labels' directory in '{self.path}'.\"\n\n    # Get file stems\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    mask_extensions = {\".png\", \".jpg\", \".jpeg\", \".bmp\", \".tiff\", \".tif\"}\n\n    image_stems = {\n        f.stem for f in images_dir.iterdir()\n        if f.is_file() and f.suffix.lower() in image_extensions\n    }\n    mask_stems = {\n        f.stem for f in labels_dir.iterdir()\n        if f.is_file() and f.suffix.lower() in mask_extensions\n    }\n\n    if not image_stems:\n        return False, f\"No image files found in '{images_dir}'.\"\n\n    if not mask_stems:\n        return False, f\"No mask files found in '{labels_dir}'.\"\n\n    # Check for matching files\n    missing_masks = image_stems - mask_stems\n    missing_images = mask_stems - image_stems\n\n    if missing_masks:\n        return False, f\"Missing binary masks in '{labels_dir}' for {len(missing_masks)} images.\"\n\n    if missing_images:\n        return False, f\"Missing images in '{images_dir}' for {len(missing_images)} masks.\"\n\n    return True, \"\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask.get_classes","title":"<code>get_classes()</code>","text":"<p>Get class names from classes.txt if it exists.</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>def get_classes(self) -&gt; Optional[dict]:\n    \"\"\"Get class names from classes.txt if it exists.\"\"\"\n    classes_file = self.path / \"classes.txt\"\n    if not classes_file.exists():\n        return None\n\n    try:\n        with open(classes_file, 'r') as f:\n            class_names = [line.strip() for line in f.readlines() if line.strip()]\n        return {i: name for i, name in enumerate(class_names)}\n    except Exception:\n        return None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask.get_image_paths","title":"<code>get_image_paths()</code>","text":"<p>Get list of original image file paths.</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>def get_image_paths(self) -&gt; List[Path]:\n    \"\"\"Get list of original image file paths.\"\"\"\n    images_dir = self.path / \"images\"\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    return [\n        f for f in images_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in image_extensions\n    ]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask.get_mask_path","title":"<code>get_mask_path(image_path)</code>","text":"<p>Get the mask file path for a given image.</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>def get_mask_path(self, image_path: Path) -&gt; Optional[Path]:\n    \"\"\"Get the mask file path for a given image.\"\"\"\n    labels_dir = self.path / \"labels\"\n    mask_extensions = [\".png\", \".jpg\", \".jpeg\", \".bmp\", \".tiff\", \".tif\"]\n\n    for ext in mask_extensions:\n        mask_path = labels_dir / f\"{image_path.stem}{ext}\"\n        if mask_path.exists():\n            return mask_path\n    return None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.binmask.Binmask.get_mask_paths","title":"<code>get_mask_paths()</code>","text":"<p>Get list of mask file paths.</p> Source code in <code>src/cvtoolkit/formats/binmask.py</code> <pre><code>def get_mask_paths(self) -&gt; List[Path]:\n    \"\"\"Get list of mask file paths.\"\"\"\n    labels_dir = self.path / \"labels\"\n    mask_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    return [\n        f for f in labels_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in mask_extensions\n    ]\n</code></pre>"},{"location":"api/formats/#yolo","title":"YOLO","text":""},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo","title":"<code>cvtoolkit.formats.yolo.Yolo</code>","text":"<p>               Bases: <code>Format</code></p> <p>Base class for YOLO format validation.</p> <p>Provides common validation logic for both segmentation and detection variants.</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>@register_format(FormatType.YOLO)\nclass Yolo(Format):\n    \"\"\"\n    Base class for YOLO format validation.\n\n    Provides common validation logic for both segmentation and detection variants.\n    \"\"\"\n\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate that the directory contains required YOLO structure.\n\n        Checks for:\n        - images/ directory\n        - labels/ directory\n        - classes.txt file\n        - Matching image/label file pairs\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        images_dir = self.path / \"images\"\n        labels_dir = self.path / \"labels\"\n        classes_file = self.path / \"classes.txt\"\n\n        # Check required directories\n        if not images_dir.exists():\n            return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n        if not labels_dir.exists():\n            return False, f\"Missing 'labels' directory in '{self.path}'.\"\n\n        if not classes_file.exists():\n            return False, f\"Missing 'classes.txt' file in '{self.path}'.\"\n\n        # Check for matching files\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        image_stems = {\n            f.stem for f in images_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in image_extensions\n        }\n        label_stems = {\n            f.stem for f in labels_dir.iterdir() \n            if f.is_file() and f.suffix.lower() == \".txt\"\n        }\n\n        if not image_stems:\n            return False, f\"No image files found in '{images_dir}'.\"\n\n        missing_labels = image_stems - label_stems\n        missing_images = label_stems - image_stems\n\n        if missing_labels:\n            return False, f\"Missing label files in '{labels_dir}' for {len(missing_labels)} images.\"\n\n        if missing_images:\n            return False, f\"Missing image files in '{images_dir}' for {len(missing_images)} labels.\"\n\n        return True, \"\"\n\n    def get_classes(self) -&gt; Optional[dict]:\n        \"\"\"Get class names from classes.txt as a dictionary.\"\"\"\n        classes_file = self.path / \"classes.txt\"\n        return parse_classes(classes_file) if classes_file.exists() else None\n\n    def get_class_list(self) -&gt; Optional[List[str]]:\n        \"\"\"Get class names as a list.\"\"\"\n        classes = self.get_classes()\n        if classes is None:\n            return None\n        return [classes[i] for i in sorted(classes.keys())]\n\n    def get_image_paths(self) -&gt; List[Path]:\n        \"\"\"Get list of image file paths.\"\"\"\n        images_dir = self.path / \"images\"\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        return [\n            f for f in images_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in image_extensions\n        ]\n\n    def get_label_path(self, image_path: Path) -&gt; Path:\n        \"\"\"Get the label file path for a given image.\"\"\"\n        return self.path / \"labels\" / f\"{image_path.stem}.txt\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo.validate_structure","title":"<code>validate_structure()</code>","text":"<p>Validate that the directory contains required YOLO structure.</p> <p>Checks for: - images/ directory - labels/ directory - classes.txt file - Matching image/label file pairs</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def validate_structure(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate that the directory contains required YOLO structure.\n\n    Checks for:\n    - images/ directory\n    - labels/ directory\n    - classes.txt file\n    - Matching image/label file pairs\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    images_dir = self.path / \"images\"\n    labels_dir = self.path / \"labels\"\n    classes_file = self.path / \"classes.txt\"\n\n    # Check required directories\n    if not images_dir.exists():\n        return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n    if not labels_dir.exists():\n        return False, f\"Missing 'labels' directory in '{self.path}'.\"\n\n    if not classes_file.exists():\n        return False, f\"Missing 'classes.txt' file in '{self.path}'.\"\n\n    # Check for matching files\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    image_stems = {\n        f.stem for f in images_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in image_extensions\n    }\n    label_stems = {\n        f.stem for f in labels_dir.iterdir() \n        if f.is_file() and f.suffix.lower() == \".txt\"\n    }\n\n    if not image_stems:\n        return False, f\"No image files found in '{images_dir}'.\"\n\n    missing_labels = image_stems - label_stems\n    missing_images = label_stems - image_stems\n\n    if missing_labels:\n        return False, f\"Missing label files in '{labels_dir}' for {len(missing_labels)} images.\"\n\n    if missing_images:\n        return False, f\"Missing image files in '{images_dir}' for {len(missing_images)} labels.\"\n\n    return True, \"\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo.get_classes","title":"<code>get_classes()</code>","text":"<p>Get class names from classes.txt as a dictionary.</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def get_classes(self) -&gt; Optional[dict]:\n    \"\"\"Get class names from classes.txt as a dictionary.\"\"\"\n    classes_file = self.path / \"classes.txt\"\n    return parse_classes(classes_file) if classes_file.exists() else None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo.get_class_list","title":"<code>get_class_list()</code>","text":"<p>Get class names as a list.</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def get_class_list(self) -&gt; Optional[List[str]]:\n    \"\"\"Get class names as a list.\"\"\"\n    classes = self.get_classes()\n    if classes is None:\n        return None\n    return [classes[i] for i in sorted(classes.keys())]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo.get_image_paths","title":"<code>get_image_paths()</code>","text":"<p>Get list of image file paths.</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def get_image_paths(self) -&gt; List[Path]:\n    \"\"\"Get list of image file paths.\"\"\"\n    images_dir = self.path / \"images\"\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    return [\n        f for f in images_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in image_extensions\n    ]\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.yolo.Yolo.get_label_path","title":"<code>get_label_path(image_path)</code>","text":"<p>Get the label file path for a given image.</p> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def get_label_path(self, image_path: Path) -&gt; Path:\n    \"\"\"Get the label file path for a given image.\"\"\"\n    return self.path / \"labels\" / f\"{image_path.stem}.txt\"\n</code></pre>"},{"location":"api/formats/#label-studio","title":"Label Studio","text":""},{"location":"api/formats/#cvtoolkit.formats.ls.LabelStudio","title":"<code>cvtoolkit.formats.ls.LabelStudio</code>","text":"<p>               Bases: <code>Format</code></p> <p>Label Studio format definition and validation.</p> Label Studio datasets expect the following structure <p>dataset/ \u251c\u2500\u2500 images/           # Directory containing image files \u251c\u2500\u2500 task.json         # JSON file with annotation tasks \u2514\u2500\u2500 template.label_config.xml  # Labeling configuration (optional)</p> Source code in <code>src/cvtoolkit/formats/ls.py</code> <pre><code>@register_format(FormatType.LABEL_STUDIO)\nclass LabelStudio(Format):\n    \"\"\"\n    Label Studio format definition and validation.\n\n    Label Studio datasets expect the following structure:\n        dataset/\n        \u251c\u2500\u2500 images/           # Directory containing image files\n        \u251c\u2500\u2500 task.json         # JSON file with annotation tasks\n        \u2514\u2500\u2500 template.label_config.xml  # Labeling configuration (optional)\n    \"\"\"\n\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate that the directory contains required Label Studio structure.\n\n        Checks for:\n        - images/ directory with at least one image file\n        - At least one .json task file\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        images_dir = self.path / \"images\"\n\n        # Check images directory\n        if not images_dir.exists():\n            return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n        if not images_dir.is_dir():\n            return False, f\"'images' is not a directory in '{self.path}'.\"\n\n        # Check for at least one image\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        image_files = [\n            f for f in images_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in image_extensions\n        ]\n\n        if not image_files:\n            return False, f\"No image files found in '{images_dir}'.\"\n\n        # Check for JSON task file\n        json_files = list(self.path.glob(\"*.json\"))\n        if not json_files:\n            return False, f\"No JSON task file found in '{self.path}'.\"\n\n        return True, \"\"\n\n    def get_task_file(self) -&gt; Path | None:\n        \"\"\"Get the first JSON task file in the dataset directory.\"\"\"\n        json_files = list(self.path.glob(\"*.json\"))\n        return json_files[0] if json_files else None\n\n    def get_config_file(self) -&gt; Path | None:\n        \"\"\"Get the label configuration XML file if it exists.\"\"\"\n        xml_files = list(self.path.glob(\"*.xml\"))\n        return xml_files[0] if xml_files else None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ls.LabelStudio.validate_structure","title":"<code>validate_structure()</code>","text":"<p>Validate that the directory contains required Label Studio structure.</p> <p>Checks for: - images/ directory with at least one image file - At least one .json task file</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/formats/ls.py</code> <pre><code>def validate_structure(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate that the directory contains required Label Studio structure.\n\n    Checks for:\n    - images/ directory with at least one image file\n    - At least one .json task file\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    images_dir = self.path / \"images\"\n\n    # Check images directory\n    if not images_dir.exists():\n        return False, f\"Missing 'images' directory in '{self.path}'.\"\n\n    if not images_dir.is_dir():\n        return False, f\"'images' is not a directory in '{self.path}'.\"\n\n    # Check for at least one image\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    image_files = [\n        f for f in images_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in image_extensions\n    ]\n\n    if not image_files:\n        return False, f\"No image files found in '{images_dir}'.\"\n\n    # Check for JSON task file\n    json_files = list(self.path.glob(\"*.json\"))\n    if not json_files:\n        return False, f\"No JSON task file found in '{self.path}'.\"\n\n    return True, \"\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ls.LabelStudio.get_task_file","title":"<code>get_task_file()</code>","text":"<p>Get the first JSON task file in the dataset directory.</p> Source code in <code>src/cvtoolkit/formats/ls.py</code> <pre><code>def get_task_file(self) -&gt; Path | None:\n    \"\"\"Get the first JSON task file in the dataset directory.\"\"\"\n    json_files = list(self.path.glob(\"*.json\"))\n    return json_files[0] if json_files else None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ls.LabelStudio.get_config_file","title":"<code>get_config_file()</code>","text":"<p>Get the label configuration XML file if it exists.</p> Source code in <code>src/cvtoolkit/formats/ls.py</code> <pre><code>def get_config_file(self) -&gt; Path | None:\n    \"\"\"Get the label configuration XML file if it exists.\"\"\"\n    xml_files = list(self.path.glob(\"*.xml\"))\n    return xml_files[0] if xml_files else None\n</code></pre>"},{"location":"api/formats/#ultralytics","title":"Ultralytics","text":""},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics","title":"<code>cvtoolkit.formats.ul.Ultralytics</code>","text":"<p>               Bases: <code>Format</code></p> <p>Base class for Ultralytics YOLO format validation.</p> <p>Provides common validation logic for both segmentation and detection variants.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>@register_format(FormatType.ULTRALYTICS)\nclass Ultralytics(Format):\n    \"\"\"\n    Base class for Ultralytics YOLO format validation.\n\n    Provides common validation logic for both segmentation and detection variants.\n    \"\"\"\n\n    REQUIRED_SPLITS = [\"train\", \"val\"]\n    OPTIONAL_SPLITS = [\"test\"]\n\n    def validate_structure(self) -&gt; Tuple[bool, str]:\n        \"\"\"\n        Validate that the directory contains required Ultralytics structure.\n\n        Checks for:\n        - YAML configuration file\n        - Required split directories (train, val)\n        - images/ and labels/ subdirectories in each split\n\n        Returns:\n            Tuple of (success: bool, message: str)\n        \"\"\"\n        # Check for YAML file\n        yaml_file = self.find_yaml_file()\n        if yaml_file is None:\n            return False, f\"No YAML configuration file found in '{self.path}'.\"\n\n        # Validate YAML contents\n        yaml_valid, yaml_msg = self._validate_yaml(yaml_file)\n        if not yaml_valid:\n            return False, yaml_msg\n\n        # Check required split directories\n        for split in self.REQUIRED_SPLITS:\n            split_valid, split_msg = self._validate_split_dir(split)\n            if not split_valid:\n                return False, split_msg\n\n        # Optionally validate test split if it exists\n        test_dir = self.path / \"test\"\n        if test_dir.exists():\n            test_valid, test_msg = self._validate_split_dir(\"test\")\n            if not test_valid:\n                return False, test_msg\n\n        return True, \"\"\n\n    def _validate_split_dir(self, split_name: str) -&gt; Tuple[bool, str]:\n        \"\"\"Validate a split directory (train/val/test) structure.\"\"\"\n        split_dir = self.path / split_name\n\n        if not split_dir.exists():\n            return False, f\"Missing required '{split_name}' directory in '{self.path}'.\"\n\n        images_dir = split_dir / \"images\"\n        labels_dir = split_dir / \"labels\"\n\n        if not images_dir.exists():\n            return False, f\"Missing 'images' directory in '{split_dir}'.\"\n\n        if not labels_dir.exists():\n            return False, f\"Missing 'labels' directory in '{split_dir}'.\"\n\n        # Check for matching image/label pairs\n        image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n        image_stems = {\n            f.stem for f in images_dir.iterdir() \n            if f.is_file() and f.suffix.lower() in image_extensions\n        }\n        label_stems = {\n            f.stem for f in labels_dir.iterdir() \n            if f.is_file() and f.suffix.lower() == \".txt\"\n        }\n\n        if not image_stems:\n            return False, f\"No image files found in '{images_dir}'.\"\n\n        missing_labels = image_stems - label_stems\n        if missing_labels and len(missing_labels) &gt; len(image_stems) * 0.1:\n            # Allow up to 10% missing labels (could be images without annotations)\n            return False, f\"Many label files missing in '{labels_dir}'. Missing: {len(missing_labels)}/{len(image_stems)}\"\n\n        return True, \"\"\n\n    def _validate_yaml(self, yaml_path: Path) -&gt; Tuple[bool, str]:\n        \"\"\"Validate the YAML configuration file.\"\"\"\n        try:\n            with open(yaml_path, 'r') as f:\n                yaml_data = yaml.safe_load(f)\n\n            if not yaml_data:\n                return False, f\"YAML file '{yaml_path}' is empty.\"\n\n            # Check for required keys\n            if 'names' not in yaml_data:\n                return False, f\"YAML file missing 'names' key (class names).\"\n\n            if 'train' not in yaml_data and 'val' not in yaml_data:\n                return False, f\"YAML file missing 'train' or 'val' path specifications.\"\n\n            return True, \"\"\n        except yaml.YAMLError as e:\n            return False, f\"Invalid YAML file '{yaml_path}': {e}\"\n        except Exception as e:\n            return False, f\"Error reading YAML file '{yaml_path}': {e}\"\n\n    def find_yaml_file(self) -&gt; Optional[Path]:\n        \"\"\"Find the dataset YAML configuration file.\"\"\"\n        yaml_files = list(self.path.glob('*.yaml')) + list(self.path.glob('*.yml'))\n        return yaml_files[0] if yaml_files else None\n\n    def read_yaml_data(self) -&gt; Optional[dict]:\n        \"\"\"Read and parse the YAML configuration file.\"\"\"\n        yaml_file = self.find_yaml_file()\n        if yaml_file is None:\n            return None\n\n        try:\n            with open(yaml_file, 'r') as f:\n                return yaml.safe_load(f)\n        except Exception:\n            return None\n\n    def get_class_names(self) -&gt; Optional[List[str]]:\n        \"\"\"Extract class names from the YAML configuration.\"\"\"\n        yaml_data = self.read_yaml_data()\n        if yaml_data is None or 'names' not in yaml_data:\n            return None\n\n        names_data = yaml_data['names']\n        if isinstance(names_data, dict):\n            return [name for _, name in sorted(names_data.items())]\n        elif isinstance(names_data, list):\n            return names_data\n        return None\n\n    def get_splits(self) -&gt; List[str]:\n        \"\"\"Get list of available splits in the dataset.\"\"\"\n        splits = []\n        for split in self.REQUIRED_SPLITS + self.OPTIONAL_SPLITS:\n            if (self.path / split).exists():\n                splits.append(split)\n        return splits\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.REQUIRED_SPLITS","title":"<code>REQUIRED_SPLITS = ['train', 'val']</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.OPTIONAL_SPLITS","title":"<code>OPTIONAL_SPLITS = ['test']</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.validate_structure","title":"<code>validate_structure()</code>","text":"<p>Validate that the directory contains required Ultralytics structure.</p> <p>Checks for: - YAML configuration file - Required split directories (train, val) - images/ and labels/ subdirectories in each split</p> <p>Returns:</p> Type Description <code>Tuple[bool, str]</code> <p>Tuple of (success: bool, message: str)</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def validate_structure(self) -&gt; Tuple[bool, str]:\n    \"\"\"\n    Validate that the directory contains required Ultralytics structure.\n\n    Checks for:\n    - YAML configuration file\n    - Required split directories (train, val)\n    - images/ and labels/ subdirectories in each split\n\n    Returns:\n        Tuple of (success: bool, message: str)\n    \"\"\"\n    # Check for YAML file\n    yaml_file = self.find_yaml_file()\n    if yaml_file is None:\n        return False, f\"No YAML configuration file found in '{self.path}'.\"\n\n    # Validate YAML contents\n    yaml_valid, yaml_msg = self._validate_yaml(yaml_file)\n    if not yaml_valid:\n        return False, yaml_msg\n\n    # Check required split directories\n    for split in self.REQUIRED_SPLITS:\n        split_valid, split_msg = self._validate_split_dir(split)\n        if not split_valid:\n            return False, split_msg\n\n    # Optionally validate test split if it exists\n    test_dir = self.path / \"test\"\n    if test_dir.exists():\n        test_valid, test_msg = self._validate_split_dir(\"test\")\n        if not test_valid:\n            return False, test_msg\n\n    return True, \"\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics._validate_split_dir","title":"<code>_validate_split_dir(split_name)</code>","text":"<p>Validate a split directory (train/val/test) structure.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def _validate_split_dir(self, split_name: str) -&gt; Tuple[bool, str]:\n    \"\"\"Validate a split directory (train/val/test) structure.\"\"\"\n    split_dir = self.path / split_name\n\n    if not split_dir.exists():\n        return False, f\"Missing required '{split_name}' directory in '{self.path}'.\"\n\n    images_dir = split_dir / \"images\"\n    labels_dir = split_dir / \"labels\"\n\n    if not images_dir.exists():\n        return False, f\"Missing 'images' directory in '{split_dir}'.\"\n\n    if not labels_dir.exists():\n        return False, f\"Missing 'labels' directory in '{split_dir}'.\"\n\n    # Check for matching image/label pairs\n    image_extensions = {\".jpg\", \".jpeg\", \".png\", \".bmp\", \".tiff\", \".tif\"}\n    image_stems = {\n        f.stem for f in images_dir.iterdir() \n        if f.is_file() and f.suffix.lower() in image_extensions\n    }\n    label_stems = {\n        f.stem for f in labels_dir.iterdir() \n        if f.is_file() and f.suffix.lower() == \".txt\"\n    }\n\n    if not image_stems:\n        return False, f\"No image files found in '{images_dir}'.\"\n\n    missing_labels = image_stems - label_stems\n    if missing_labels and len(missing_labels) &gt; len(image_stems) * 0.1:\n        # Allow up to 10% missing labels (could be images without annotations)\n        return False, f\"Many label files missing in '{labels_dir}'. Missing: {len(missing_labels)}/{len(image_stems)}\"\n\n    return True, \"\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics._validate_yaml","title":"<code>_validate_yaml(yaml_path)</code>","text":"<p>Validate the YAML configuration file.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def _validate_yaml(self, yaml_path: Path) -&gt; Tuple[bool, str]:\n    \"\"\"Validate the YAML configuration file.\"\"\"\n    try:\n        with open(yaml_path, 'r') as f:\n            yaml_data = yaml.safe_load(f)\n\n        if not yaml_data:\n            return False, f\"YAML file '{yaml_path}' is empty.\"\n\n        # Check for required keys\n        if 'names' not in yaml_data:\n            return False, f\"YAML file missing 'names' key (class names).\"\n\n        if 'train' not in yaml_data and 'val' not in yaml_data:\n            return False, f\"YAML file missing 'train' or 'val' path specifications.\"\n\n        return True, \"\"\n    except yaml.YAMLError as e:\n        return False, f\"Invalid YAML file '{yaml_path}': {e}\"\n    except Exception as e:\n        return False, f\"Error reading YAML file '{yaml_path}': {e}\"\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.find_yaml_file","title":"<code>find_yaml_file()</code>","text":"<p>Find the dataset YAML configuration file.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def find_yaml_file(self) -&gt; Optional[Path]:\n    \"\"\"Find the dataset YAML configuration file.\"\"\"\n    yaml_files = list(self.path.glob('*.yaml')) + list(self.path.glob('*.yml'))\n    return yaml_files[0] if yaml_files else None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.read_yaml_data","title":"<code>read_yaml_data()</code>","text":"<p>Read and parse the YAML configuration file.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def read_yaml_data(self) -&gt; Optional[dict]:\n    \"\"\"Read and parse the YAML configuration file.\"\"\"\n    yaml_file = self.find_yaml_file()\n    if yaml_file is None:\n        return None\n\n    try:\n        with open(yaml_file, 'r') as f:\n            return yaml.safe_load(f)\n    except Exception:\n        return None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.get_class_names","title":"<code>get_class_names()</code>","text":"<p>Extract class names from the YAML configuration.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def get_class_names(self) -&gt; Optional[List[str]]:\n    \"\"\"Extract class names from the YAML configuration.\"\"\"\n    yaml_data = self.read_yaml_data()\n    if yaml_data is None or 'names' not in yaml_data:\n        return None\n\n    names_data = yaml_data['names']\n    if isinstance(names_data, dict):\n        return [name for _, name in sorted(names_data.items())]\n    elif isinstance(names_data, list):\n        return names_data\n    return None\n</code></pre>"},{"location":"api/formats/#cvtoolkit.formats.ul.Ultralytics.get_splits","title":"<code>get_splits()</code>","text":"<p>Get list of available splits in the dataset.</p> Source code in <code>src/cvtoolkit/formats/ul.py</code> <pre><code>def get_splits(self) -&gt; List[str]:\n    \"\"\"Get list of available splits in the dataset.\"\"\"\n    splits = []\n    for split in self.REQUIRED_SPLITS + self.OPTIONAL_SPLITS:\n        if (self.path / split).exists():\n            splits.append(split)\n    return splits\n</code></pre>"},{"location":"api/utils/","title":"API Reference \u2014 Utilities","text":"<p>This page documents the utility modules for mask handling and RLE encoding/decoding.</p>"},{"location":"api/utils/#mask-utilities","title":"Mask Utilities","text":""},{"location":"api/utils/#cvtoolkit.mask","title":"<code>cvtoolkit.mask</code>","text":"<p>Mask utilities for converting between binary masks and YOLO format.</p>"},{"location":"api/utils/#cvtoolkit.mask.log","title":"<code>log = logging.getLogger('MaskToYolo')</code>  <code>module-attribute</code>","text":""},{"location":"api/utils/#cvtoolkit.mask.TaskType","title":"<code>TaskType</code>","text":"<p>               Bases: <code>Enum</code></p> Source code in <code>src/cvtoolkit/formats/__init__.py</code> <pre><code>class TaskType(Enum):\n    GENERIC = \"Generic\"\n    DETECTION = \"Detection\"\n    SEGMENTATION = \"Segmentation\"\n</code></pre>"},{"location":"api/utils/#cvtoolkit.mask.seg_to_bbox","title":"<code>seg_to_bbox(seg_info)</code>","text":"<p>Convert a segmentation label in YOLO format to a bounding box label.</p> <p>Parameters:</p> Name Type Description Default <code>seg_info</code> <code>list</code> <p>A list containing segmentation information.       - The first element is the class ID.       - The remaining elements are pairs of (x, y) coordinates defining the polygon.</p> required <p>Returns:</p> Type Description <code>list</code> <p>A bounding box representation in YOLO format as:</p> <code>list</code> <p>[classid, xcenter, y_center, width, height]</p> <code>list</code> <ul> <li>class_id: The zero-based class index.</li> </ul> <code>list</code> <ul> <li>x_center: The normalized x-coordinate of the bbox center.</li> </ul> <code>list</code> <ul> <li>y_center: The normalized y-coordinate of the bbox center.</li> </ul> <code>list</code> <ul> <li>width: The normalized width of the bbox.</li> </ul> <code>list</code> <ul> <li>height: The normalized height of the bbox.</li> </ul> Source code in <code>src/cvtoolkit/formats/yolo.py</code> <pre><code>def seg_to_bbox(seg_info: list) -&gt; list:\n    \"\"\"Convert a segmentation label in YOLO format to a bounding box label.\n\n    Args:\n        seg_info: A list containing segmentation information.\n                  - The first element is the class ID.\n                  - The remaining elements are pairs of (x, y) coordinates defining the polygon.\n\n    Returns:\n        A bounding box representation in YOLO format as:\n        [class_id, x_center, y_center, width, height]\n        - class_id: The zero-based class index.\n        - x_center: The normalized x-coordinate of the bbox center.\n        - y_center: The normalized y-coordinate of the bbox center.\n        - width: The normalized width of the bbox.\n        - height: The normalized height of the bbox.\n    \"\"\"\n    class_id, *points = seg_info\n\n    points = [float(p) for p in points]\n\n    x_min, y_min = min(points[0::2]), min(points[1::2])\n    x_max, y_max = max(points[0::2]), max(points[1::2])\n\n    width, height = x_max - x_min, y_max - y_min\n    x_center, y_center = (x_min + x_max) / 2, (y_min + y_max) / 2\n\n    bbox_info = [int(class_id), x_center, y_center, width, height]\n\n    return bbox_info\n</code></pre>"},{"location":"api/utils/#cvtoolkit.mask.mask_to_yolo","title":"<code>mask_to_yolo(mask, task_type=TaskType.GENERIC)</code>","text":"<p>Convert a binary mask to YOLO segmentation or bounding box format.</p> <p>Parameters:</p> Name Type Description Default <code>mask</code> <code>ndarray</code> <p>Binary mask where 255 represents the object and 0 represents the background.</p> required <code>task_type</code> <code>TaskType</code> <p>The type of annotation to generate (SEGMENTATION or DETECTION).</p> <code>GENERIC</code> <p>Returns:</p> Type Description <p>A list containing YOLO-formatted segmentation or bounding box annotations.</p> <p>Each annotation starts with class_id (0 for binary masks) followed by coordinates.</p> Source code in <code>src/cvtoolkit/mask.py</code> <pre><code>def mask_to_yolo(mask: np.ndarray, task_type: TaskType = TaskType.GENERIC):\n    \"\"\"Convert a binary mask to YOLO segmentation or bounding box format.\n\n    Args:\n        mask: Binary mask where 255 represents the object and 0 represents the background.\n        task_type: The type of annotation to generate (SEGMENTATION or DETECTION).\n\n    Returns:\n        A list containing YOLO-formatted segmentation or bounding box annotations.\n        Each annotation starts with class_id (0 for binary masks) followed by coordinates.\n    \"\"\"\n    img_height, img_width = mask.shape\n\n    yolo_lines = []\n\n    # Find contours in the binary mask\n    binary_mask = (mask == 255).astype(np.uint8)\n    contours, _ = cv2.findContours(\n        binary_mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE\n    )\n\n    # log.info(f\"Found {len(contours)} contours, task_type={task_type}\")\n\n    for idx, contour in enumerate(contours):\n        # log.info(f\"  Contour {idx}: shape={contour.shape}, len={len(contour)}\")\n\n        if len(contour) &lt; 3:\n            # log.info(f\"    Skipping: contour has &lt; 3 points\")\n            continue\n\n        contour = contour.squeeze()\n        # log.info(f\"    After squeeze: shape={contour.shape}\")\n\n        # Handle case where squeeze reduces to 1D (single point - shouldn't happen with &gt;= 3)\n        if contour.ndim == 1:\n            # log.warning(f\"    Skipping: contour became 1D after squeeze\")\n            continue\n\n        # Start with class ID 0 (binary masks have single class)\n        seg_line = [0]\n\n        for point in contour:\n            seg_line.append(round(point[0] / img_width, 6))\n            seg_line.append(round(point[1] / img_height, 6))\n\n        if task_type == TaskType.SEGMENTATION:\n            yolo_lines.append(seg_line)\n            # log.info(f\"    Added segmentation line with {len(seg_line)} elements\")\n        elif task_type == TaskType.DETECTION:\n            bbox_line = seg_to_bbox(seg_line)\n            yolo_lines.append(bbox_line)\n            # log.info(f\"    Added detection bbox: {bbox_line}\")\n        else:\n            # log.info(f\"    Skipping: task_type={task_type} not SEGMENTATION or DETECTION\")\n            continue\n\n    return yolo_lines\n</code></pre>"},{"location":"api/utils/#rle-utilities","title":"RLE Utilities","text":""},{"location":"api/utils/#cvtoolkit.rle","title":"<code>cvtoolkit.rle</code>","text":"<p>Run-Length Encoding (RLE) utilities for mask conversion.</p> <p>Provides encoding and decoding functions for converting between binary masks and Label Studio's RLE format.</p>"},{"location":"api/utils/#cvtoolkit.rle.InputStream","title":"<code>InputStream</code>","text":"<p>A simple input stream for reading bits from binary data.</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>class InputStream:\n    \"\"\"A simple input stream for reading bits from binary data.\"\"\"\n\n    def __init__(self, data: str):\n        \"\"\"Initialize with binary data string (0s and 1s).\"\"\"\n        self.data = data\n        self.i = 0\n\n    def read(self, size: int) -&gt; int:\n        \"\"\"Read specified number of bits and convert to integer.\"\"\"\n        out = self.data[self.i: self.i + size]\n        self.i += size\n        return int(out, 2)\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.InputStream.__init__","title":"<code>__init__(data)</code>","text":"<p>Initialize with binary data string (0s and 1s).</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def __init__(self, data: str):\n    \"\"\"Initialize with binary data string (0s and 1s).\"\"\"\n    self.data = data\n    self.i = 0\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.InputStream.read","title":"<code>read(size)</code>","text":"<p>Read specified number of bits and convert to integer.</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def read(self, size: int) -&gt; int:\n    \"\"\"Read specified number of bits and convert to integer.\"\"\"\n    out = self.data[self.i: self.i + size]\n    self.i += size\n    return int(out, 2)\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.access_bit","title":"<code>access_bit(data, num)</code>","text":"<p>Access a specific bit from bytes array.</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def access_bit(data: bytes, num: int) -&gt; int:\n    \"\"\"Access a specific bit from bytes array.\"\"\"\n    base = int(num // 8)\n    shift = 7 - int(num % 8)\n    return (data[base] &amp; (1 &lt;&lt; shift)) &gt;&gt; shift\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.bits2byte","title":"<code>bits2byte(arr_str, n=8)</code>","text":"<p>Convert bits string back to bytes list.</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def bits2byte(arr_str: str, n: int = 8) -&gt; list:\n    \"\"\"Convert bits string back to bytes list.\"\"\"\n    rle = []\n    numbers = [arr_str[i: i + n] for i in range(0, len(arr_str), n)]\n    for i in numbers:\n        rle.append(int(i, 2))\n    return rle\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.bytes2bit","title":"<code>bytes2bit(data)</code>","text":"<p>Convert bytes data to bit string.</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def bytes2bit(data: bytes) -&gt; str:\n    \"\"\"Convert bytes data to bit string.\"\"\"\n    return \"\".join([str(access_bit(data, i)) for i in range(len(data) * 8)])\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.base_rle_encode","title":"<code>base_rle_encode(inarray)</code>","text":"<p>Run length encoding for input array.</p> <p>Returns:</p> Type Description <code>tuple</code> <p>Tuple of (runlengths, startpositions, values)</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def base_rle_encode(inarray: np.ndarray) -&gt; tuple:\n    \"\"\"\n    Run length encoding for input array.\n\n    Returns:\n        Tuple of (runlengths, startpositions, values)\n    \"\"\"\n    ia = np.asarray(inarray)\n    n = len(ia)\n\n    if n == 0:\n        return None, None, None\n    else:\n        y = ia[1:] != ia[:-1]\n        i = np.append(np.where(y), n - 1)\n        z = np.diff(np.append(-1, i))\n        p = np.cumsum(np.append(0, z))[:-1]\n        return z, p, ia[i]\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.encode_rle","title":"<code>encode_rle(arr, wordsize=8, rle_sizes=None)</code>","text":"<p>Encode a 1D array to run length encoding (RLE).</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>Flattened numpy array from a 4D image (R, G, B, alpha)</p> required <code>wordsize</code> <code>int</code> <p>Wordsize bits for decoding</p> <code>8</code> <code>rle_sizes</code> <code>list</code> <p>List of integers for run lengths</p> <code>None</code> <p>Returns:</p> Type Description <code>list</code> <p>Run length encoded array</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def encode_rle(arr: np.ndarray, wordsize: int = 8, rle_sizes: list = None) -&gt; list:\n    \"\"\"\n    Encode a 1D array to run length encoding (RLE).\n\n    Args:\n        arr: Flattened numpy array from a 4D image (R, G, B, alpha)\n        wordsize: Wordsize bits for decoding\n        rle_sizes: List of integers for run lengths\n\n    Returns:\n        Run length encoded array\n    \"\"\"\n    if rle_sizes is None:\n        rle_sizes = [3, 4, 8, 16]\n\n    num = len(arr)\n    numbits = f\"{num:032b}\"\n\n    wordsizebits = f\"{wordsize - 1:05b}\"\n    rle_bits = \"\".join([f\"{x - 1:04b}\" for x in rle_sizes])\n    base_str = numbits + wordsizebits + rle_bits\n    out_str = \"\"\n\n    for length_reeks, p, value in zip(*base_rle_encode(arr)):\n        if length_reeks == 1:\n            out_str += \"0\"\n            out_str += \"00\"\n            out_str += \"000\"\n            out_str += f\"{value:08b}\"\n\n        elif length_reeks &gt; 1:\n            if length_reeks &lt;= 8:\n                out_str += \"1\"\n                out_str += \"00\"\n                out_str += f\"{length_reeks - 1:03b}\"\n                out_str += f\"{value:08b}\"\n\n            elif 8 &lt; length_reeks &lt;= 16:\n                out_str += \"1\"\n                out_str += \"01\"\n                out_str += f\"{length_reeks - 1:04b}\"\n                out_str += f\"{value:08b}\"\n\n            elif 16 &lt; length_reeks &lt;= 256:\n                out_str += \"1\"\n                out_str += \"10\"\n                out_str += f\"{length_reeks - 1:08b}\"\n                out_str += f\"{value:08b}\"\n\n            else:\n                length_temp = length_reeks\n                while length_temp &gt; 2 ** 16:\n                    out_str += \"1\"\n                    out_str += \"11\"\n                    out_str += f\"{2 ** 16 - 1:016b}\"\n                    out_str += f\"{value:08b}\"\n                    length_temp -= 2 ** 16\n\n                out_str += \"1\"\n                out_str += \"11\"\n                out_str += f\"{length_temp - 1:016b}\"\n                out_str += f\"{value:08b}\"\n\n    nzfill = 8 - len(base_str + out_str) % 8\n    total_str = base_str + out_str\n    total_str = total_str + nzfill * \"0\"\n\n    rle = bits2byte(total_str)\n\n    return rle\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.decode_rle","title":"<code>decode_rle(rle)</code>","text":"<p>Decode an RLE-encoded list to a flattened numpy uint8 image.</p> <p>Parameters:</p> Name Type Description Default <code>rle</code> <code>list</code> <p>RLE-encoded list of integers</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Flattened numpy uint8 image [width, height, channel]</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def decode_rle(rle: list) -&gt; np.ndarray:\n    \"\"\"\n    Decode an RLE-encoded list to a flattened numpy uint8 image.\n\n    Args:\n        rle: RLE-encoded list of integers\n\n    Returns:\n        Flattened numpy uint8 image [width, height, channel]\n    \"\"\"\n    input_ = InputStream(bytes2bit(rle))\n    num = input_.read(32)\n    word_size = input_.read(5) + 1\n    rle_sizes = [input_.read(4) + 1 for _ in range(4)]\n\n    i = 0\n    out = np.zeros(num, dtype=np.uint8)\n    while i &lt; num:\n        x = input_.read(1)\n        j = i + 1 + input_.read(rle_sizes[input_.read(2)])\n        if x:\n            val = input_.read(word_size)\n            out[i:j] = val\n            i = j\n        else:\n            while i &lt; j:\n                val = input_.read(word_size)\n                out[i] = val\n                i += 1\n    return out\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.mask_to_rle","title":"<code>mask_to_rle(mask)</code>","text":"<p>Convert mask to RLE format.</p> <p>Parameters:</p> Name Type Description Default <code>mask</code> <code>ndarray</code> <p>uint8 or int numpy array mask with len(shape) == 2 (grayscale)</p> required <p>Returns:</p> Type Description <code>list</code> <p>List of integers in RLE format</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def mask_to_rle(mask: np.ndarray) -&gt; list:\n    \"\"\"\n    Convert mask to RLE format.\n\n    Args:\n        mask: uint8 or int numpy array mask with len(shape) == 2 (grayscale)\n\n    Returns:\n        List of integers in RLE format\n    \"\"\"\n    assert len(mask.shape) == 2, \"mask must be 2D np.array\"\n    assert mask.dtype == np.uint8 or mask.dtype == int, \"mask must be uint8 or int\"\n    array = mask.ravel()\n    array = np.repeat(array, 4)\n    rle = encode_rle(array)\n    return rle\n</code></pre>"},{"location":"api/utils/#cvtoolkit.rle.yolo_to_mask","title":"<code>yolo_to_mask(contour, img_width, img_height)</code>","text":"<p>Convert a YOLO segmentation mask (polygon format) into a uint8 2D mask.</p> <p>Parameters:</p> Name Type Description Default <code>contour</code> <code>list</code> <p>List of normalized polygon points [x1, y1, x2, y2, ..., xn, yn]</p> required <code>img_width</code> <code>int</code> <p>Original image width</p> required <code>img_height</code> <code>int</code> <p>Original image height</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>2D numpy array (uint8) representing the segmentation mask</p> Source code in <code>src/cvtoolkit/rle.py</code> <pre><code>def yolo_to_mask(contour: list, img_width: int, img_height: int) -&gt; np.ndarray:\n    \"\"\"\n    Convert a YOLO segmentation mask (polygon format) into a uint8 2D mask.\n\n    Args:\n        contour: List of normalized polygon points [x1, y1, x2, y2, ..., xn, yn]\n        img_width: Original image width\n        img_height: Original image height\n\n    Returns:\n        2D numpy array (uint8) representing the segmentation mask\n    \"\"\"\n    polygon = np.array(contour, dtype=np.float32).reshape(-1, 2)\n    polygon[:, 0] *= img_width\n    polygon[:, 1] *= img_height\n    polygon = polygon.astype(np.int32)\n\n    mask = np.zeros((img_height, img_width), dtype=np.uint8)\n    cv2.fillPoly(img=mask, pts=[polygon], color=[255])\n\n    return mask\n</code></pre>"},{"location":"conversions/overview/","title":"Conversions Overview","text":"<p>AgribotTools supports bidirectional conversions between all four annotation formats, with YOLO acting as the central interchange hub.</p>"},{"location":"conversions/overview/#conversion-matrix","title":"Conversion Matrix","text":"Input Format Output Format Function Tasks BinMask YOLO <code>binmask_to_yolo()</code> S / OD YOLO BinMask <code>binmask_to_yolo(reverse)</code> S BinMask Label Studio <code>binmask_to_ls()</code> S / OD YOLO Label Studio <code>yolo_to_ls()</code> S / OD Label Studio YOLO <code>yolo_to_ls(reverse)</code> S / OD YOLO Ultralytics <code>yolo_to_ul()</code> S / OD Ultralytics YOLO <code>yolo_to_ul(reverse)</code> S / OD Label Studio Ultralytics <code>ls_to_ul()</code> S / OD Ultralytics Label Studio <code>ls_to_ul(reverse)</code> S / OD <p>Tasks: S = Segmentation, OD = Object Detection.</p>"},{"location":"conversions/overview/#conversion-workflow","title":"Conversion Workflow","text":"<p>The following diagram shows all available conversion paths:</p> <pre><code>flowchart TB\n    subgraph S[\"Segmentation\"]\n        direction LR\n        YOLOS(YOLO-S) -- \"yolo_to_ls(s)\" --&gt; LSS(LS-S)\n        LSS -- \"yolo_to_ls(s, reverse)\" --&gt; YOLOS\n        YOLOS -- \"yolo_to_ul(s)\" --&gt; ULS(UL-S)\n        ULS -- \"yolo_to_ul(s, reverse)\" --&gt; YOLOS\n        LSS -- \"ls_to_ul(s)\" --&gt; ULS\n        ULS -- \"ls_to_ul(s, reverse)\" --&gt; LSS\n    end\n    subgraph OD[\"Object Detection\"]\n        direction LR\n        YOLOD(YOLO-OD) -- \"yolo_to_ls(d)\" --&gt; LSD(LS-OD)\n        LSD -- \"yolo_to_ls(d, reverse)\" --&gt; YOLOD\n        YOLOD -- \"yolo_to_ul(d)\" --&gt; ULD(UL-OD)\n        ULD -- \"yolo_to_ul(d, reverse)\" --&gt; YOLOD\n        LSD -- \"ls_to_ul(d)\" --&gt; ULD\n        ULD -- \"ls_to_ul(d, reverse)\" --&gt; LSD\n    end\n    BM(BinMask) -- \"binmask_to_yolo(s)\" --&gt; YOLOS\n    BM -- \"binmask_to_yolo(d)\" --&gt; YOLOD\n    YOLOS -- \"binmask_to_yolo(s, reverse)\" --&gt; BM\n    style BM fill:#03ab\n    style S fill:#b03a\n    style OD fill:#ab03</code></pre>"},{"location":"conversions/overview/#key-concepts","title":"Key Concepts","text":""},{"location":"conversions/overview/#validation","title":"Validation","text":"<p>Before any conversion runs, AgribotTools automatically validates the source dataset structure to catch issues early (missing folders, wrong file types, etc.).</p>"},{"location":"conversions/overview/#rollback","title":"Rollback","text":"<p>If a conversion fails mid-way, AgribotTools automatically rolls back any partially created files and directories to keep your filesystem clean.</p>"},{"location":"conversions/usage/","title":"Conversions Usage","text":"<p>AgribotTools provides two ways to run format conversions: CLI scripts and the Python API.</p>"},{"location":"conversions/usage/#cli-scripts","title":"CLI Scripts","text":"<p>Ready-to-use scripts are located in the <code>scripts/</code> directory. Each script handles a specific conversion and accepts command-line arguments.</p>"},{"location":"conversions/usage/#available-scripts","title":"Available Scripts","text":"Script Conversion <code>binmask_to_yolo.py</code> BinMask \u2192 YOLO <code>yolo_to_binmask.py</code> YOLO \u2192 BinMask <code>binmask_to_ls.py</code> BinMask \u2192 Label Studio <code>yolo_to_ls.py</code> YOLO \u2192 Label Studio <code>ls_to_yolo.py</code> Label Studio \u2192 YOLO <code>yolo_to_ul.py</code> YOLO \u2192 Ultralytics <code>ul_to_yolo.py</code> Ultralytics \u2192 YOLO <code>ls_to_ul.py</code> Label Studio \u2192 Ultralytics <code>ul_to_ls.py</code> Ultralytics \u2192 Label Studio <code>seg_ls_to_bbox_ls.py</code> Label Studio segmentation \u2192 Label Studio detection <code>import_tasks_ls.py</code> Bulk import tasks into Label Studio"},{"location":"conversions/usage/#example-yolo-label-studio","title":"Example: YOLO \u2192 Label Studio","text":"<pre><code>python scripts/yolo_to_ls.py seg /path/to/yolo_dataset --ls_base_name my_output\n</code></pre>"},{"location":"conversions/usage/#example-yolo-ultralytics","title":"Example: YOLO \u2192 Ultralytics","text":"<pre><code>python scripts/yolo_to_ul.py /path/to/yolo_dataset \\\n    --ul_path /path/to/output \\\n    --split_ratios 0.8 0.1 0.1 \\\n    --include_test_split \\\n    --image_ext .jpg,.png \\\n    --random_seed 42\n</code></pre>"},{"location":"conversions/usage/#getting-help","title":"Getting Help","text":"<p>To see all available options for any script:</p> <pre><code>python scripts/&lt;script_name&gt;.py -h\n</code></pre>"},{"location":"conversions/usage/#python-api","title":"Python API","text":"<p>For programmatic usage, you can use the conversion classes directly.</p>"},{"location":"conversions/usage/#using-the-formatregistry-recommended","title":"Using the FormatRegistry (Recommended)","text":"<p>The <code>FormatRegistry</code> provides a dynamic lookup of available conversions:</p> <pre><code>from cvtoolkit.formats.format import FormatType, FormatRegistry\n\n# ...\n\n# Get the conversion class\nconverter_class = FormatRegistry.get_conversion_class(\n    FormatType.YOLO, \n    FormatType.LABEL_STUDIO\n)\n\n# Run the conversion\nconverter = converter_class(\n    source_path=\"/path/to/yolo_dataset\",\n    target_path=\"/path/to/output\"\n)\nresult = converter.run()\nprint(f\"Converted to: {result}\")\n</code></pre>"},{"location":"conversions/usage/#using-conversion-classes-directly","title":"Using Conversion Classes Directly","text":"<p>You can also import and use specific conversion classes:</p> <pre><code>from cvtoolkit.conversions.yolo_to_ul import YoloToUltralytics\nfrom cvtoolkit.formats import TaskType\n\nconverter = YoloToUltralytics(\n    source_path=\"/path/to/yolo_dataset\",\n    target_path=\"/path/to/output\",\n    task_type=TaskType.SEGMENTATION\n)\n\nresult = converter.run(\n    split_ratios=(0.8, 0.1, 0.1),\n    include_test_split=True,\n    image_ext=\".jpg,.png\",\n    random_seed=42,\n)\n</code></pre> <p>&lt;!-- ### Reverse Conversions</p> <p>For reversible conversions, use <code>run_reverse()</code>:</p> <pre><code>from cvtoolkit.conversions.yolo_to_ul import YoloToUltralytics\nfrom cvtoolkit.formats import TaskType\n\n# Note: source/target are swapped for reverse\nconverter = YoloToUltralytics(\n    source_path=\"/path/to/ul_dataset\",\n    target_path=\"/path/to/yolo_output\",\n    task_type=TaskType.GENERIC\n)\nresult = converter.run_reverse()\n``` --&gt;\n\n### Progress Tracking\n\nConversions support progress callbacks for integration with GUIs or logging:\n\n```python\ndef on_progress(progress: float, message: str):\n    print(f\"[{progress:.0%}] {message}\")\n\nconverter.set_progress_callback(on_progress)\nconverter.run()\n</code></pre>"},{"location":"conversions/usage/#error-handling","title":"Error Handling","text":"<p>Conversions raise <code>ConversionError</code> on failure:</p> <pre><code>from cvtoolkit.conversions.conversion import ConversionError\n\ntry:\n    result = converter.run()\nexcept ConversionError as e:\n    print(f\"Conversion failed: {e}\")\n    # Partial files are automatically rolled back\n</code></pre>"},{"location":"formats/binmask/","title":"Binary Mask Format","text":"<p>The Binary Mask (BinMask) format stores segmentation labels as binary PNG images where each pixel value encodes a class ID.</p>"},{"location":"formats/binmask/#directory-structure","title":"Directory Structure","text":"<pre><code>binmask_dset/\n\u251c\u2500\u2500 images/\n\u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u251c\u2500\u2500 img_02.png\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 labels/\n\u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u251c\u2500\u2500 img_02.png\n\u2502   \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 classes.txt\n</code></pre>"},{"location":"formats/binmask/#components","title":"Components","text":"Component Description <code>images/</code> Labelled images in <code>jpg</code> or <code>png</code> format. <code>labels/</code> One binary <code>png</code> mask per image. Each pixel's intensity value corresponds to a class ID. <code>classes.txt</code> Text file listing class names, one per line. The line index corresponds to the class ID used in the masks."},{"location":"formats/binmask/#mask-encoding","title":"Mask Encoding","text":"<p>In the label images:</p> <ul> <li>A pixel value of <code>0</code> represents background.</li> <li>A pixel value of <code>255</code> represents foreground (for single-class binary masks).</li> <li>For multi-class masks, each pixel value corresponds to a class index from <code>classes.txt</code>.</li> </ul>"},{"location":"formats/binmask/#supported-tasks","title":"Supported Tasks","text":"Task Supported Segmentation \u2705 Object Detection \u2705 (via contour extraction)"},{"location":"formats/binmask/#available-conversions","title":"Available Conversions","text":"Direction Function BinMask \u2192 YOLO <code>binmask_to_yolo()</code> <p>| BinMask \u2192 Label Studio | <code>binmask_to_ls()</code> |</p> <p>See the Conversions Usage Guide for CLI and API examples.</p>"},{"location":"formats/label-studio/","title":"Label Studio Format","text":"<p>The Label Studio (LS) format is the native annotation format used by Label Studio, an open-source data labeling tool.</p>"},{"location":"formats/label-studio/#directory-structure","title":"Directory Structure","text":"<pre><code>ls_dset/\n\u251c\u2500\u2500 images/\n\u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u251c\u2500\u2500 img_02.png\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 info.json\n\u2514\u2500\u2500 info.xml\n</code></pre>"},{"location":"formats/label-studio/#components","title":"Components","text":"Component Description <code>images/</code> Labelled images in <code>jpg</code> or <code>png</code> format. <code>info.json</code> JSON file containing image references and their label annotations. <code>info.xml</code> XML configuration file defining the labeling interface."},{"location":"formats/label-studio/#xml-configuration-example","title":"XML Configuration Example","text":"<p>The <code>info.xml</code> file configures the Label Studio labeling interface:</p> <pre><code>&lt;View&gt;\n    &lt;!-- View the image to be labelled --&gt;\n    &lt;Image name=\"image\" value=\"$image\" /&gt;\n    &lt;!-- Define the label classes --&gt;\n    &lt;Labels name=\"label\" toName=\"image\"&gt;\n        &lt;Label value=\"Object\" /&gt;\n    &lt;/Labels&gt;\n\n    &lt;!-- Tool for drawing bounding boxes --&gt;\n    &lt;RectangleLabels name=\"bbox\" toName=\"image\"&gt;\n        &lt;Label value=\"Object\" /&gt;\n    &lt;/RectangleLabels&gt;\n&lt;/View&gt;\n</code></pre>"},{"location":"formats/label-studio/#supported-tasks","title":"Supported Tasks","text":"Task Supported Segmentation \u2705 Object Detection \u2705 <p>Note</p> <p>The LS format supports both segmentation and object detection. The task type is determined by the label structure in the JSON file.</p>"},{"location":"formats/label-studio/#large-file-import","title":"Large File Import","text":"<p>File size limits</p> <p>Label Studio only supports JSON task files up to 250,000 tasks or 50 MB.</p> <p>For larger files, use the utility script <code>scripts/import_tasks_ls.py</code>, which automatically chunks the file and uploads parts separately to a local Label Studio instance.</p>"},{"location":"formats/label-studio/#environment-variables","title":"Environment Variables","text":"<p>Conversions involving Label Studio require specific environment variables. See the Getting Started page for configuration details.</p>"},{"location":"formats/label-studio/#available-conversions","title":"Available Conversions","text":"Direction Function YOLO \u2192 Label Studio <code>yolo_to_ls()</code> Label Studio \u2192 YOLO <code>ls_to_yolo()</code> Label Studio \u2192 Ultralytics <code>ls_to_ul()</code> Ultralytics \u2192 Label Studio <code>ul_to_ls()</code> BinMask \u2192 Label Studio <code>binmask_to_ls()</code> <p>See the Conversions Usage Guide for CLI and API examples.</p>"},{"location":"formats/overview/","title":"Formats Overview","text":"<p>AgribotTools supports four annotation formats used in computer vision for object detection and segmentation tasks.</p>"},{"location":"formats/overview/#acronyms-and-definitions","title":"Acronyms and Definitions","text":""},{"location":"formats/overview/#tools","title":"Tools","text":"Acronym Explanation LS Label Studio \u2014 the software used for labeling. UL Ultralytics \u2014 target library for object detection and segmentation. BinMask Binary mask \u2014 binary image where <code>1</code> represents a foreground pixel, <code>0</code> represents background. SegMask Segmentation mask \u2014 mask used to highlight an object of interest. Bbox Bounding box \u2014 rectangle used to highlight an object of interest."},{"location":"formats/overview/#task-types","title":"Task Types","text":"Acronym Definition Description S Segmentation A segmentation task. OD Object Detection An object detection task."},{"location":"formats/overview/#format-identifiers","title":"Format Identifiers","text":"Acronym Definition Description BinMask Binary Mask Segmentation mask for several classes in a binary format. YOLO YOLO format Standard YOLOv1-v3 annotation format. LS Label Studio Format used by Label Studio. UL Ultralytics Format used by Ultralytics."},{"location":"formats/overview/#supported-formats","title":"Supported Formats","text":"Format Segmentation Object Detection Details Binary Mask :check: :check: Binary PNG masks YOLO :check: :check: Polygon or bbox text annotations Label Studio :check: :check: JSON + XML configuration Ultralytics :check: :check: Train/val/test split with YAML config"},{"location":"formats/overview/#format-selection-guide","title":"Format Selection Guide","text":"<ul> <li>Starting from scratch? Use the YOLO format \u2014 it is the central hub for most conversions.</li> <li>Need to label data? Convert to Label Studio format and use the Label Studio UI.</li> <li>Ready to train? Convert to Ultralytics format for direct use with <code>ultralytics</code> training pipelines.</li> <li>Have binary masks from another tool? Start with Binary Mask format and convert as needed.</li> </ul>"},{"location":"formats/ultralytics/","title":"Ultralytics Format","text":"<p>The Ultralytics (UL) format is the dataset structure expected by the Ultralytics training library. It organizes data into train/val/test splits with a YAML configuration file.</p>"},{"location":"formats/ultralytics/#directory-structure","title":"Directory Structure","text":"<pre><code>ul_dset/\n\u251c\u2500\u2500 train/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 labels/\n\u2502       \u251c\u2500\u2500 img_01.txt\n\u2502       \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 val/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 labels/\n\u2502       \u251c\u2500\u2500 img_01.txt\n\u2502       \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 test/          # optional\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 labels/\n\u2502       \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 ul_dset.yaml\n</code></pre>"},{"location":"formats/ultralytics/#components","title":"Components","text":"Component Description <code>train/</code> Training split with <code>images/</code> and <code>labels/</code> subfolders. <code>val/</code> Validation split with the same structure. <code>test/</code> (Optional) Test split with the same structure. <code>ul_dset.yaml</code> YAML configuration file (named after the dataset). <p>Note</p> <p>The <code>train</code>, <code>val</code>, and <code>test</code> subfolders share the same internal structure. Labels use the YOLO text format.</p>"},{"location":"formats/ultralytics/#yaml-configuration","title":"YAML Configuration","text":"<p>The YAML file configures paths and class names for the Ultralytics training pipeline:</p> <pre><code># Dataset name\npath: /path/to/dataset              # Absolute path to dataset root\ntrain: /train/images                 # Training images (relative to path)\nval: /val/images                     # Validation images (relative to path)\ntest: /test/images                   # Test images (relative to path, optional)\n\n# Class names\nnames:\n    0: first_class\n    1: second_class\n    2: ...\n</code></pre>"},{"location":"formats/ultralytics/#supported-tasks","title":"Supported Tasks","text":"Task Supported Segmentation \u2705 Object Detection \u2705 <p>Note</p> <p>The UL format supports both segmentation and object detection. The task type is determined by the label structure (same as YOLO labels).</p>"},{"location":"formats/ultralytics/#available-conversions","title":"Available Conversions","text":"Direction Function YOLO \u2192 Ultralytics <code>yolo_to_ul()</code> Ultralytics \u2192 YOLO <code>ul_to_yolo()</code> Label Studio \u2192 Ultralytics <code>ls_to_ul()</code> Ultralytics \u2192 Label Studio <code>ul_to_ls()</code> <p>See the Conversions Usage Guide for CLI and API examples.</p>"},{"location":"formats/yolo/","title":"YOLO Format","text":"<p>The YOLO format is the standard annotation format used by YOLO models (v1\u2013v3 and compatible). It serves as the central hub in AgribotTools \u2014 most conversions pass through YOLO as an intermediate format.</p>"},{"location":"formats/yolo/#directory-structure","title":"Directory Structure","text":"<pre><code>yolo_dset/\n\u251c\u2500\u2500 images/\n\u2502   \u251c\u2500\u2500 img_01.png\n\u2502   \u251c\u2500\u2500 img_02.png\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 labels/\n\u2502   \u251c\u2500\u2500 img_01.txt\n\u2502   \u251c\u2500\u2500 img_02.txt\n\u2502   \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 classes.txt\n</code></pre>"},{"location":"formats/yolo/#components","title":"Components","text":"Component Description <code>images/</code> Labelled images in <code>jpg</code> or <code>png</code> format. <code>labels/</code> One <code>.txt</code> file per image (same filename, different extension). Each line describes one annotation. <code>classes.txt</code> Text file listing class names, one per line. Line index = class ID."},{"location":"formats/yolo/#label-format","title":"Label Format","text":"<p>Each line in a label <code>.txt</code> file describes a single annotation. The format depends on the task type:</p>"},{"location":"formats/yolo/#segmentation","title":"Segmentation","text":"<pre><code>&lt;class_id&gt; &lt;x1&gt; &lt;y1&gt; &lt;x2&gt; &lt;y2&gt; ... &lt;xn&gt; &lt;yn&gt;\n</code></pre> <p>A polygon defined by <code>n</code> normalized coordinate pairs (values in <code>[0, 1]</code> relative to image width/height).</p>"},{"location":"formats/yolo/#object-detection","title":"Object Detection","text":"<pre><code>&lt;class_id&gt; &lt;x_center&gt; &lt;y_center&gt; &lt;width&gt; &lt;height&gt;\n</code></pre> <p>A bounding box defined by its center and dimensions, all normalized to <code>[0, 1]</code>.</p>"},{"location":"formats/yolo/#supported-tasks","title":"Supported Tasks","text":"Task Supported Segmentation \u2705 Object Detection \u2705 <p>Note</p> <p>The YOLO format supports both segmentation and object detection \u2014 the label structure determines the task type.</p>"},{"location":"formats/yolo/#available-conversions","title":"Available Conversions","text":"Direction Function YOLO \u2192 Label Studio <code>yolo_to_ls()</code> Label Studio \u2192 YOLO <code>ls_to_yolo()</code> YOLO \u2192 Ultralytics <code>yolo_to_ul()</code> Ultralytics \u2192 YOLO <code>ul_to_yolo()</code> BinMask \u2192 YOLO <code>binmask_to_yolo()</code> <p>See the Conversions Usage Guide for CLI and API examples.</p>"},{"location":"gui/usage/","title":"Gradio GUI","text":"<p>AgribotTools includes a Gradio-based graphical interface for running format conversions without writing code.</p>"},{"location":"gui/usage/#launching-the-gui","title":"Launching the GUI","text":"<pre><code>python gui.py\n</code></pre> <p>The GUI will start a local web server, accessible at http://localhost:7860 by default.</p>"},{"location":"gui/usage/#server-options","title":"Server Options","text":"<p>The GUI launches with the following default settings:</p> Option Default Server address <code>0.0.0.0</code> (all interfaces) Port <code>7860</code> Public sharing Disabled"},{"location":"gui/usage/#features","title":"Features","text":"<p>The GUI provides a unified interface for:</p> <ul> <li>Selecting source and target formats \u2014 dropdown menus populated from the format registry.</li> <li>Browsing input/output directories \u2014 file pickers for source and target paths.</li> <li>Running conversions \u2014 one-click conversion with real-time progress feedback.</li> <li>Format validation \u2014 automatic validation of the source dataset before conversion.</li> <li>Error reporting \u2014 clear error messages displayed in the interface.</li> </ul>"},{"location":"gui/usage/#supported-conversions","title":"Supported Conversions","text":"<p>The GUI dynamically discovers all registered conversions. When you select a source format, only the valid target formats are shown in the target dropdown.</p> <p>This is powered by the same <code>FormatRegistry</code> used by the Python API. See the API Reference for details.</p>"},{"location":"gui/usage/#architecture","title":"Architecture","text":"<p>The GUI is implemented across three modules:</p> Module Responsibility <code>gui.py</code> Main Gradio Blocks interface creation and launch. <code>src/ui/formats.py</code> Format dropdown population from the registry. <code>src/ui/callbacks.py</code> Conversion execution callbacks with progress reporting."},{"location":"gui/usage/#base-data-root","title":"Base data root","text":"<p>By default, the data root points at the root of the filesystem. This behaviour is changed if a symlink to a <code>data</code> folder is provided within the working directory.</p>"}]}